# Х3-F — разнородная популяция для контроля эквивалентности

Закрывает две записи `FOLLOWUPS.md`: «Контроль эквивалентности силён ровно
настолько, насколько разнороден наш `src`» и «Разнородный прогон контроля
эквивалентности некуда положить». Гейт: нет.

Редакция вторая: ревью проверило посадку вердиктов исполнением и нашло, что
предложенный пол не является полом того, что пакет чинит. Разделы «Решение»,
«Контракты» и «Кейсы» переписаны целиком.

Зависимость: пакет читает `scripts/directive-narrow-control.php` в состоянии
**после** пакета C-1, который переписал его чтение отчёта.

## Что измерено до планирования

Измерение 2026-09-02, перепроверено ревью 2026-09-03. Способ: чтение скрипта,
прогон продукта, посадка вердиктов на временных деревьях вне репозитория, замер
времени, прогон cs-fixer и PHPStan по кандидату в фикстуры.

**Цель зашита сильнее, чем сказано.** Скрипт не читает ни `$argv`, ни путь к
конфигурации: цель — константа, `-c` не передаётся ни в один из двух прогонов.

**Конфигурацию, однако, прогон всё-таки видит** — через рабочий каталог: продукт
подхватывает `qmx.yaml` из корня, откуда скрипт его и запускает. Измерено: одна
и та же фикстура из корня даёт `{"inert":1,"unmeasured":2}`, а из своего каталога
`{"inert":3}`. Значит «без `--config` — умолчания продукта» было бы **сменой
смысла** сегодняшнего прогона по `src/`, а не сохранением его.

**Сегодняшняя популяция.** 43 вердикта, все `Effective`: 31 пороговый и 12
символьных. Под полом контроля ходят только пороговые.

**Сужение двигает только измеренные пороговые вердикты** — скрипт говорит это
сам. Значит пол «каждое значение `DirectiveEffect` встречается хотя бы раз»
разнородности словаря требует, а различающей популяции — нет: фикстура из шести
директив со всеми четырьмя вердиктами и двумя измеренными порогами этот пол
проходит, оставаясь вырожденной.

**Ветки коалиций прячутся не в вердикте, а в причине.** Запись заведена ровно
из-за них, а коалиция наблюдаема через одно из четырёх значений
`DirectiveUnmeasurableReason`. Пол по вердиктам её не покрывает.

**Посадка вердиктов даётся не целиком.** `Effective`, `Overrun`, `Inert` и две
причины (`already-refused` через неизвестный канал, `producer-disabled` через
выключенное правило) сажаются с первой попытки. Две причины сопротивлялись:
`@qmx-ignore — причина` без фильтра правил вообще не даёт сайта, а две
`@qmx-threshold` одного правила в одном докблоке дают один вердикт, второй молча
теряется — `masked` при этом не появляется. Рецепт на все четыре причины
считается **неизвестным** и добывается исполнением до реализации.

**Ловушки формы:** `channel=100` молча теряется (нужен пробел и `-- причина`);
`Overrun` требует явных `warning`/`error` по обе стороны измеренного значения,
короткая форма даёт `Effective`.

**Инфраструктура фикстуры проверена.** cs-fixer правилами проекта: 0 файлов к
правке, докблочные директивы не трогаются. PHPStan: каталог фикстур Inline уже
исключён целиком. PHPUnit фикстуру не обнаружит. Каталог фикстур **попадает** в
`docs/internal/generated/modular-architecture/test-ownership.tsv` — по строке на
файл, и без ручных деклараций манифеста `check:artifacts` покраснеет.

**Механизма исключений у перечислителя директив нет.** `enumerate-inline-directives.php tests`
находит четыре сайта, из них два — в существующей фикстуре; спасает только цель
`src`. Инвариант «посаженные директивы не попадают в измерения дерева» держится
соглашением, и новая фикстура его нагрузит.

**Цена.** Измерено 2026-09-03 на этом дереве: `composer directives:narrow-control`
целиком — 1 м 11 с. Из них `src/` 70.3 с (узкий 15.4, полный 54.9), фикстура
целиком 0.6 с (0.3 + 0.3), `Silenced/` 0.5 с. Прежняя редакция называла «0.27 с
прогон по фикстуре» — это был один свип, а не прогон.

## Архитектурное решение

**Контроль получает цель и конфигурацию.** Оба прогона внутри одного сравнения
обязаны видеть одно дерево и одну конфигурацию. Прогон по `src/` передаёт
корневой `qmx.yaml` **явно** — иначе пакет незаметно переопределит смысл
сегодняшнего контроля; прогон по фикстуре передаёт её собственный конфиг.

**Пол разнородности — про различающую популяцию, а не про словарь.** Он состоит
из трёх требований: измеренных пороговых вердиктов не меньше названного минимума
(это то, что сужение вообще может двигать); каждое значение `DirectiveEffect`
встречается **среди вердиктов формы `threshold`**; каждое значение
`DirectiveUnmeasurableReason` встречается по обеим формам.

Ограничение оси вердиктов формой `threshold` — правка по ревью исполнения:
символьный вердикт судится по тому, что он заглушил, а не переисполнением
правила, поэтому свипы согласуются на нём по построению. Ревью построило
популяцию, проходившую пол и вырожденную: единственный `inert` переписан как
`@qmx-ignore`. Требовать сверх формы ещё и измеренность нечего — `effective`,
`overrun` и `inert` измерены по определению.

Ось причин намеренно остаётся по обеим формам, и это **покрытие словаря, а не
различение**: `addresses-every-channel` достижим только у подавления, а
`already-refused` и `producer-disabled` присуждаются из конфигурации и каталога
каналов до того, как свип о чём-то спросят. Различающую силу на этой оси несёт
один `masked` — и ради него ось и заведена, потому что своего вердикта у него
нет.

**Фикстура живёт в репозитории и не попадает в измерения, которые судят наш
код.** Инвариант держат два теста: перечисление `@qmx-threshold` по `src/` не
содержит ни одного сайта фикстуры (сравнение по содержанию сайта, не по пути), и
ни один файл фикстуры целиком не лежит под `src/` (сравнение по хэшу). Второй
нужен потому, что первый видит только пороговую форму: посаженный
`@qmx-ignore *` не сдвинул бы ни одного перечисления, но под `src/` заглушил бы
все правила своего класса в `check`, в снимке подавлений и в ратчете.

Измерено 2026-09-03: ратчет, снимок подавлений, `bin/qmx check src/`,
перечисление директив по `src/` и корпус гейта фикстурой не задеты. **Задето
одно**: `enumeration-renames.tsv` считает текстовые вхождения имени канала по
всему дереву, и удаление каталога фикстуры двигает девять счётчиков. Исключать
каталог из этого периметра не будем: перечисление переименований отвечает на
вопрос «сколько текста придётся править», а докблок фикстуры — такой же текст,
как докблок `Fixtures/ThresholdAudit/PairedDirectives.php`, который туда входит
с самого начала. Цена — правка фикстуры требует перегенерации артефакта.

**Наш `src/` из контроля не уходит**: он про настоящую популяцию, фикстура — про
словарь. Прогонов три, и порядок их — от самого дешёвого сигнала к самому
дорогому: фикстура целиком (0.6 с), её половина `Silenced/` (0.5 с), `src/`
(70 с). Composer прекращает список на первой ненулевой команде, поэтому «код
цели — худший из трёх» неверно: это код первого упавшего прогона, и порядок
решает, какой сигнал вообще будет виден.

**Исходы «прогон невалиден» получают свой код, и гарантия формулируется над
`Throwable`, а не над списком мест.** Список пробовали дважды и оба раза он был
неполон: сперва «десять мест» пропустило «отчёт сообщает о другом свипе» и
«корень репозитория неразрешим», потом пересчёт по точкам `throw` **в этом
файле** пропустил `throw` в подключаемом хелпере — `Process::run()` бросает
`QmxFindingGate\GateError`, чей файл скрипт не подключал и автозагрузчика не
имеет, так что несостоявшийся `proc_open` давал `Fatal error: Class not found`
и 255. Лечение: `require` на `GateError.php` плюс `catch (\Throwable)`, и
обещание звучит как «любой `Throwable` отвечает кодом 3». Перечисление мест
ниже остаётся как список того, что скрипт называет своими словами, а не как
основание гарантии. Места (кодом 7 отвечает первое, остальные — кодом 3): неразбираемый JSON;
error-envelope; код вне {0,2}; расхождение кода процесса и поля `exit_code`;
**отчёт сообщает о другом свипе**; неполный охват; ноль проанализированных
файлов; пустой список вердиктов; ноль измеренных порогов; расхождение `scope`;
расхождение `selection`; разные множества сайтов; **неразрешимый корень
репозитория**. Пакет, объявляющий «255 не остаётся», обязан их перевести —
иначе это обещание без фондирования.

## Контракты

```php
// scripts/directive-narrow-control.php
--target=<path>            обязателен
--config=<path>            обязателен: молчаливое умолчание меняет смысл прогона
--require-heterogeneity    включает пол разнородности
--min-measured=<n>         минимум измеренных пороговых вердиктов (по умолчанию 1)
```

Коды: 0 — совпало; 1 — разошлось; 2 — популяция не удовлетворяет полу
разнородности; 3 — прогон невалиден (каждый сегодняшний `RuntimeException` плюс
отказ по аргументам); 7 — отчёт неузнаваемой формы (общий с гейтом после C-1).
Необработанных 255 не остаётся, и это проверяется перечислением точек `throw`,
а не утверждением.

`composer directives:narrow-control` — три последовательные команды composer, а
не одна оболочка над тремя целями, поэтому «худший из трёх» на деле означает
«первый упавший»: composer прекращает список на первой ненулевой команде. Это
сказано и в `scripts-descriptions`, и в блоке AGENTS.md, а не только в
`FOLLOWUPS.md` — читать будут их.

Прогоны, в порядке исполнения:

1. фикстура целиком, с её конфигом, `--require-heterogeneity --min-measured=3`;
2. `Silenced/` — та же фикстура без `OverrunBoundary.php`, с тем же конфигом и
   без пола: популяция, на которой сужение не тем правилом доходит до сравнения
   вердиктов;
3. `src/` с корневым `qmx.yaml`, без требования разнородности.

Фикстура: `tests/Analysis/Policy/Inline/Fixtures/NarrowControl/` плюс её
`qmx.yaml`, один на оба прогона по ней:

```
NarrowControl/
├── qmx.yaml
├── EveryChannelSuppression.php   # @qmx-ignore * — единственный файл без порога
├── OverrunBoundary.php           # пол его требует; прогон 2 обходится без него
├── UnjudgeableThresholds.php     # already-refused, producer-disabled
└── Silenced/
    ├── LiveAndDeadDirectives.php # effective + inert
    └── MaskingPair.php           # masked × 2
```

### Скелет фикстуры, добытый исполнением 2026-09-03

Каждая строка проверена прогоном `bin/qmx directives <dir> -c <dir>/qmx.yaml`
до написания кода контроля. Носитель — метод с семью параметрами, который
`code-smell.long-parameter-list` репортит по умолчанию и чья цикломатика равна
единице.

| Исход                                    | Форма                                                                                                                                                                                                            |
| ---------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `Effective`                              | `@qmx-threshold code-smell.long-parameter-list warning=9 error=12 -- ...` над семипараметровым методом                                                                                                           |
| `Inert`                                  | `@qmx-threshold complexity.cyclomatic warning=50 error=80 -- ...` над прямолинейным методом                                                                                                                      |
| `Overrun`                                | `@qmx-threshold code-smell.long-parameter-list warning=3 error=4 -- ...` над тем же методом: обе границы названы и обе ниже семи                                                                                 |
| `Unmeasured` / `masked`                  | один и тот же `@qmx-threshold code-smell.long-parameter-list warning=9 error=12` в докблоке класса **и** в докблоке метода: класс материализуется на каждом callable, снятие любого поодиночке ничего не двигает |
| `Unmeasured` / `already-refused`         | `@qmx-threshold narrow-control.no-such-channel warning=1 error=2 -- ...`: канала нет ни у одного продюсера                                                                                                       |
| `Unmeasured` / `producer-disabled`       | `@qmx-threshold complexity.cognitive warning=50 error=80 -- ...` при `disabled_rules: [complexity.cognitive]` в своём `qmx.yaml`                                                                                 |
| `Unmeasured` / `addresses-every-channel` | `@qmx-ignore * -- ...` на классе                                                                                                                                                                                 |

Две формы, о которые разбились прежние попытки, названы явно: причина
`addresses-every-channel` требует **звёздочки** (`@qmx-ignore * -- ...`), а не
голого `@qmx-ignore -- ...`, который сайта не даёт вовсе; `masked` требует двух
директив на **разных строках** (класс плюс метод), потому что группировка идёт
по паре «строка + правило» и две одинаковые директивы в одном докблоке
схлопываются в один сайт.

Два правила выключены в `qmx.yaml` фикстуры: `complexity.cognitive` — предмет
`producer-disabled`, `coupling.distance` — ради тишины, оно предупреждает на
stderr про дерево без проектных неймспейсов.

Измеренная популяция: 8 вердиктов, `effective=1 overrun=1 inert=1 unmeasured=5`,
причины `producer-disabled=1 already-refused=1 addresses-every-channel=1
masked=2`, измеренных порогов 3 — отсюда `--min-measured=3`.

## Кейсы и граничные условия

- `Overrun` требует обеих границ; тест проверяет именно `Overrun`, а не «не
  `Effective`».
- Все четыре причины неизмеримости присутствуют. Если какая-то окажется
  недостижимой из авторского кода — это **находка**, и она идёт в
  `FOLLOWUPS.md` вместе с сужением пола, а не тихо выпадает из фикстуры.
- Фикстура чиста по PER-CS (cs-fixer её форматирует) и не обнаруживается
  PHPUnit.
- Каталог фикстуры не попадает в `enumerate-inline-directives.php src` и в
  сверку популяции гейта. В `enumeration-renames.tsv` он попадает, и это принято
  осознанно — см. раздел «Архитектурное решение».
- Манифест: ручных деклараций новые файлы фикстуры не потребовали — инвентарь
  выводит владельца из пути; `test-ownership.tsv` перегенерируется.
- У популяции, удовлетворяющей полу, есть предел: она обязана нести `Overrun`,
  `Overrun` обязан нести сработавшую находку, а сработавшая находка включает
  третий контроль аудита раньше любого вердикта. Поэтому дефект «сужать не тем
  правилом» на ней даёт код 3. Второй прогон — по `Silenced/`, где каждое
  адресуемое правило заглушено своей директивой, — доводит тот же дефект до
  сравнения вердиктов.
- Прогон по фикстуре детерминирован: никаких путей вне её каталога и никакой
  зависимости от корневого конфига.

## Definition of Done

1. `composer directives:narrow-control` — 0, и в выводе видно все три прогона с
   их целями и конфигами; текст «MATCH» называет цель. Порядок — от дешёвого
   сигнала к дорогому, потому что composer останавливается на первом упавшем.
2. Прогон по фикстуре печатает распределение вердиктов и причин; все четыре
   вердикта и все четыре причины ненулевые, измеренных порогов не меньше
   минимума. Числа — из прогона.
3. Пол проверен снятием трижды: убери единственный `Overrun` → код 2; убери
   единственную причину `masked` → код 2; опусти число измеренных порогов ниже
   минимума → код 2. Каждый раз возврат правки даёт 0.
4. Мутация «сужать всегда не тем правилом» (та самая, что раньше проходила
   зелёной) прогнана на обеих популяциях, на клоне с физически скопированным
   `vendor`: на фикстуре целиком — код 3 (третий контроль аудита отказывает
   раньше вердиктов), на `Silenced/` — код 1 **сравнением вердиктов**. Второе и
   есть закрытие; первое объясняется, а не выдаётся за него.
5. Места, дававшие непонятный код, перечислены поимённо по точкам `throw`, и
   для трёх из них показано исполнением, что теперь возвращается 3.
6. Оба барьера — «перечисление по `src/` не содержит сайтов фикстуры» и «ни
   один файл фикстуры не лежит под `src/`» — зелёные и проверены снятием:
   временная копия порогового файла краснит первый, копия файла с одним
   `@qmx-ignore *` — второй.
7. `QMX_MKDOCS=<основной checkout>/website/.venv/bin/mkdocs composer check` — 0.
8. `composer directives:controls` — 0.
9. Цена названа: секунды каждого из трёх прогонов, измеренные.

## Что пакет не закрывает

Корпус `finding-gate/` остаётся вне этого контроля: он намеренно внешний, а
директивы здесь — предмет нашего дерева. Инвариант «фикстуры не попадают в
перечисление» держится тестом, а не механизмом самого перечислителя; запись об
этом заводится тем же коммитом.
