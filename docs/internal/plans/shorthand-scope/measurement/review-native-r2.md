# Ревью плана X24, раунд 2 — находки (reviewer: claude)

Предмет — ПЛАН на коммите `dd27e1c0` ветки `x24-shorthand-scope`, дельта к `47b940ec`
(две ревизии: `365885e3`, `dd27e1c0`) плюс четыре новых файла измерения. Кода лечения
ещё нет, поэтому «после лечения» везде выводится из контракта плюс измеренной сегодняшней
базы, а не из работающего лечения.

**Дерево не менялось мной.** Все пробы — в `mktemp -d` и в `<review>/raw/`. Однако
`git status` в конце ревью НЕ пуст: параллельный писатель этой же сессии правил
`01-contract.md`, `02-cure.md`, `03-acceptance.md` в 06:20–06:21, уже после того как я
снял материал. Я ревьюирую **закоммиченный** `dd27e1c0`, как предписано брифом, и по
каждой находке отдельно указываю, закрывает ли её незакоммиченная правка (она закрывает
claude-r2-05 целиком и половину claude-r2-01).

## Гипотеза брифа — короткий ответ

**Подтверждена частично.** По частям:

- «ревизии 2–3 закрыли все находки раунда 1» — **почти**: девять из десяти закрыты,
  claude-05 на `dd27e1c0` НЕ закрыта (закрыта незакоммиченной правкой), claude-09 закрыта
  наполовину;
- «не открыв новых» — **опровергнуто**: две HIGH, обе подтверждены прогоном;
- «P0 необходим» — **подтверждено кодом**: `effectWritesFor()` возвращает одну
  каноническую величину на форму (`forms.tsv`: int → 7331), так что обе стороны пары
  пишут одно значение;
- «P0 достаточен, чтобы число оси B что-то значило» — **опровергнуто**: остаётся второй,
  независимый механизм холостого зеленения (claude-r2-04), а собственные числа плана,
  на которые P0 опирается, под P0 не пересчитаны (claude-r2-01);
- «таблица назначений на 465 строк полна и верна» — **значения верны**: строки с неверно
  назначенным `coexistence` я не нашёл, проверив все 21 структурную форму назначаемых
  ячеек вручную против C1–C4. Неверна **сводка** этой таблицы в `03-acceptance.md`
  (claude-r2-01, claude-r2-07).

---

### claude-r2-01

- **reviewer**: claude
- **severity**: HIGH
- **kind**: contract
- **domain**: reliability
- **title**: Предсказание «0 дефектов и 15 NOT OBSERVABLE, на популяции, какой её оставил P0» противоречит самому P0: девять из пятнадцати — ровно та коллизия величин, которую P0 убирает; измеренное число — 6, а не 15
- **mechanism**: `03-acceptance.md` делает P0 первым пакетом именно потому, что стенд пишет одну каноническую величину на числовой ключ и обе стороны пары спорят за общий указатель ОДНИМ значением. Та же секция дальше называет пятнадцать ячеек, которые «после лечения читаются NOT OBSERVABLE, и это честное чтение», и объясняет три из них — `complexity.{ccn,cognitive,npath} | callable: × threshold` — словами «это свойство контракта, а не величин: после лечения верхняя полоса не трогает полосу `callable`, которую написал блок, поэтому две стороны становятся идентичными документами». Объяснение неверно: документы `{callable: {warning: X, error: X}}` и `{threshold: Y}` дают идентичные ОБЪЕКТЫ только при X == Y, то есть ровно из-за канонической величины. `Classifier::pair()` уходит в NOT OBSERVABLE по гейту чувствительности `$onlyA->text === $onlyB->text`, и при различимых величинах гейт не срабатывает. То же верно для шести строк `callable.threshold × threshold` (kinds 2-same-name и 4-cross-level). Итого девять из пятнадцати — артефакт величин, который P0 по своему же DoD устраняет. Собственное измерение захода это знает и говорит обратное плану: `axis-b-values.md` §13 — «`Stand::effectWritesFor()` возвращает один канонический литерал на объявленную форму, так что полоса блока и развёрнутая полоса сокращения равны ПО ПОСТРОЕНИЮ для каждого числового ключа», и §13 же формулирует настоящий ремонт как «`pairSide()` должен выбирать величину второй стороны так, чтобы она отличалась от первой» — это и есть P0. Хедж «эти числа стоят при канонических величинах и могут сдвинуться, когда P0 сделает стороны различимыми» стоит только у таблицы значений 80/27/17/4 и не распространён ни на пятнадцать, ни на «четырнадцать строк, которые сегодняшние значения покрасят в красное», ни на разбиение «39 зелены и на нелеченом продукте / 74 несут доказательство». Все три числа сняты при канонических величинах и под P0 сдвинутся; назвать их новые значения нельзя ни этим ревью, ни планом, потому что под P0 сдвигается и колонка «сегодня» — ровно те 33 зелёные ячейки нелеченого продукта, которые §14 объясняет коллизией 7331. Пересчитать их можно только прогоном стенда после P0, и до него ни одно из трёх не является рубриком.
- **trigger**: воспроизводится в нормальной работе — первый же прогон стенда после P0, то есть на первом приёмочном рубеже раунда
- **in_scope**: да (`03-acceptance.md:95-113`, `:127-140`)
- **anchor**: `docs/internal/plans/shorthand-scope/03-acceptance.md:95-113` (пятнадцать и их объяснение), `:55-62` (таблица значений), `:87-93` (четырнадцать), `:127-140` (сетка); `scripts/promise-effect/Classifier.php:288-292` (гейт `one-wins`), `scripts/promise-effect/Stand.php:1467-1520` (`effectWritesFor`)
- **evidence**:
  ```php
  // Classifier::pair(), ветка one-wins — гейт чувствительности
  if ($onlyA->text === $onlyB->text || $expected->text === $omitted->text) {
      return new Judgement(Verdict::NOT_OBSERVABLE, 'the two sides render the same, ...');
  }
  ```
  Прогон: тот же пробник захода (`axis-b-all.php`) с одной правкой — вторая
  (верхнеключевая) сторона переписана на вторую величину, то есть ровно то, что
  обещает P0 (`<review>/raw/p0-sim.php`, `p0-sim.summary`):
  ```
  без P0 (как в плане)     assigned=one-wins  COEXISTENCE_OK 6   NOT OBSERVABLE 11
                           assigned=deeper    COEXISTENCE_OK 76  NOT OBSERVABLE 4     => всего 15 NO
  с P0 (различимые)        assigned=one-wins  COEXISTENCE_OK 15  NOT OBSERVABLE 2
                           assigned=deeper    COEXISTENCE_OK 76  NOT OBSERVABLE 4     => всего 6 NO
  дефектов после лечения: 0 в обоих случаях
  ```
  Девять перешедших из NOT OBSERVABLE в COEXISTENCE_OK — это ровно
  `complexity.{ccn,cognitive,npath} | callable: × threshold` и
  `… | callable.threshold × threshold` в обоих kind'ах.
- **verification**: confirmed
- **verification_note**: прогон сделан на продуктовом `Classifier::pair()` (пропатченная копия дерева захода `/tmp/x24-dw-cezvVV/tree`, ветка `deeper-wins`), на замороженных наблюдениях самого продукта `docs/internal/generated/promise-effect/observations-before/raw.tsv`. Зеркальный вариант (вторую величину получает ГЛУБОКАЯ сторона, а не верхняя) даёт то же самое — 6 NOT OBSERVABLE, `<review>/raw/p0-sim-mirror.tsv`, — так что результат не артефакт выбора стороны. Оставшиеся шесть — четыре `complexity.npath | *.enabled × enabled` (класс-уровень npath по умолчанию выключен) и две `coupling.cbo | class.scope × scope` (перечисление из двух слов) — действительно не спасаются никакой величиной.
- **fix_direction**: пересчитать ВСЕ производные числа раздела при различимых величинах и назвать их одним прогоном, а не по частям: пятнадцать, четырнадцать, 39/74 и таблицу 80/27/17/4; для трёх строк `callable: × threshold` снять объяснение «свойство контракта» и заменить его измеренным; в самой сетке писать число, которое пересобранная таблица даёт ДО прогона, а не направление. Незакоммиченная правка рабочего дерева уже сделала последнее (сетка теперь ссылается на «пересобранную таблицу» и прямо говорит, что «15 NOT OBSERVABLE» написано из таблицы, которую P0 обесценивает), но тело раздела — пятнадцать с их объяснением, четырнадцать и таблица значений — не тронуто, и документ спорит сам с собой.

---

### claude-r2-02

- **reviewer**: claude
- **severity**: HIGH
- **kind**: contract
- **domain**: reliability
- **title**: P4 удаляет единственный механизм, которым сегодня отказывает верхнеуровневая смесь спеллингов у coupling-семейства: C4 обещает, что ни один существующий отказ не убран, K5 сторожит другой случай, а документа нет ни в популяции, ни в оси B
- **mechanism**: Документ `coupling.cbo: {threshold: 30, warning: 10}` в ОДНОМ слое — это ровно то, что C4 называет отказом («два спеллинга одной полосы, в одном массиве, в одном слое») и что `01-contract.md` в разделе «What this answer does NOT change» обещает сохранить дословно. Сегодня отказ работает так: `RuleOptionThresholdShorthand::unfold()` по условию 3 НЕ разворачивает сокращение, когда в том же слое написан градуированный ключ («это внутрислойная смесь, и она обязана дойти до `ThresholdParser`»), поэтому оба верхних ключа доживают до `fromArray()`, где плоская ветка зовёт `ThresholdParser::parse($config, WARNING, ERROR, 14, 20)` и отказывает. Единственный верхнеуровневый вызов `parse()` у `CboOptions` и у `InstabilityOptions` лежит ВНУТРИ этой ветки — той самой, которую P4 объявляет удаляемой («Both early branches go»). Посылка, на которой построено удаление, — «`fromArray()` получает документ, в котором не выжила ни одна верхнеуровневая полоса» — для этого документа ложна по построению: условие 3 существует затем, чтобы полоса выжила. Что делает с ней новый шаг подачи P3 («push the value into every reached level whose own group this layer left unwritten, then remove the top-level key»), план не говорит НИГДЕ, и ни одну из двух реализаций не выбирает. Та, что повторяет сегодняшнее условие 3 (не подавать, потому что в слое написаны оба спеллинга), после P4 даёт молчаливую потерю обоих ключей — это и снято прогоном. Вторая (подать смесь в уровни) отказ сохранила бы — сообщение `ThresholdParser` уровня не называет, я это проверил отдельно, — но она противоречит второй половине C2 («the top-level key fills only what that layer left unwritten») и переносит отказ на путь, которого автор не писал; цена и последствия этого выбора в плане не посчитаны. Приёмка это не поймает: верхней формы, пишущей два спеллинга одной полосы, нет ни в `population.tsv` (T0–T6), ни в `population-gap.tsv` (добавлен только T7 — половина полосы), а в леджере оси B четыре строки `refuse` — это пары ДВУХ верхних ключей, судимые как `both->outcome === REFUSED_FRAMED`, то есть они краснеют только если стенд вообще дойдёт до этого документа; для complexity-семейства таких строк нет вовсе.
- **trigger**: воспроизводится в нормальной работе — `qmx.yaml`, в котором у `coupling.cbo`/`coupling.instability` рядом стоят `threshold` и `warning`/`max_warning`; сегодня это громкий отказ с exit 3
- **in_scope**: да (`01-contract.md:73-81`, `:327`, `02-cure.md:164-182`)
- **anchor**: `docs/internal/plans/shorthand-scope/01-contract.md:73-81` (C4), `:327` («does NOT change»), `02-cure.md:164-182` (P4), `:171` («Both early branches go»); `src/Analysis/Evidence/Coupling/CboOptions.php:61-83`, `src/Analysis/Evidence/Coupling/InstabilityOptions.php:53-71`, `src/Analysis/Finding/RuleConfiguration/RuleOptionThresholdShorthand.php:122-131` (условие 3)
- **evidence**:
  ```
  # тот же документ, тот же фикстур, две сборки продукта
  PRISTINE (dd27e1c0)                exit=3
    Configuration error: Cannot mix "threshold" with "warning"/"error". ...
  P4-СИМУЛЯЦИЯ (плоская ветка CboOptions::fromArray() удалена, копия дерева) exit=2
    4 violations (3 errors, 1 warning)      # отказа нет, оба ключа молча потеряны
  # контроль на той же паре сборок: {threshold: 30} в одиночку -> exit=2 в обеих
  # и отдельно: сообщение об отказе уровня не называет — та же строка на
  # `class: {threshold: 30, warning: 10}` -> exit=3, текст тот же
  ```
  То же сообщение на втором правиле семейства, на нетронутом продукте:
  `coupling.instability: {threshold: 0.9, max_warning: 0.5}` → exit 3,
  `Cannot mix "threshold" with "max_warning"/"max_error"`.
- **verification**: confirmed
- **verification_note**: копия дерева построена по предписанию брифа (`git ls-files` + `rsync`, `vendor/` скопирован, НЕ симлинк — иначе PSR-4 резолвился бы в исходное дерево и правка была бы невидима), правка — удаление ровно плоской ветки `CboOptions::fromArray()`, 927 символов. Оба прогона — из каталога вне репозитория, `git status` репозитория правкой не затронут. Для complexity-семейства этот дефект НЕ воспроизводится и я это проверил отдельно: там `warning` вообще не принимается на верхнем уровне, отказ даёт распознавание (`Option "warning" is not an option of rule "complexity.ccn"`), а распознавание работает после подачи и переживёт её — см. claude-r2-03.
- **fix_direction**: назвать этот документ явно как отдельный случай контракта и решить его одним из двух способов, а не оставить на усмотрение исполнителя: либо подача пропускает группу, в которой в этом слое написаны оба спеллинга, и тогда P4 обязан сохранить верхнеуровневый вызов `ThresholdParser::parse()` (а не «удалить ветку целиком»), либо отказ переносится на шов подачи и называет верхние ключи — и тогда посылка «`fromArray()` получает документ без верхнеуровневой полосы» становится истинной, а не декларативной. В любом случае добавить форму «два спеллинга одной полосы на верхнем уровне» в популяцию, по которой пишется DoD P3, и расширить K5 с «слот уровня неправильной формы» до «подача не удаляет ни один сегодняшний отказ», потому что сегодняшняя формулировка K5 про этот случай молчит.

---

### claude-r2-03

- **reviewer**: claude
- **severity**: MEDIUM
- **kind**: contract
- **domain**: architecture
- **title**: Таблица «Reach, per rule» — та самая декларация, которую пишет P3, — приписывает трём complexity-правилам верхнеуровневый спеллинг `warning`+`error`, которого продукт не принимает; и гейт `LONE_THRESHOLD`, который сегодня пропускает эту группу в `unfold()`, не назван ни в контракте, ни в P3
- **mechanism**: Раздел «Reach, per rule» открывается словами «Reach is what the cure must declare. The table below is the claim», и её колонка «top-level band» для `complexity.ccn/cognitive/npath` читает «`threshold` / `warning`+`error`». Верхний уровень этих трёх правил принимает только `enabled, threshold, callable, class` — снято рефлексией с `acceptedOptionKeys()`, — а реестр объявляет на пути `''` именно `LONE_THRESHOLD_SHAPE` с ПУСТЫМИ списками `warning`/`error` и комментарием «The empty `warning`/`error` lists are load-bearing». Отсюда два следствия, которых план не несёт. Первое: вторая половина C2 — «Symmetrically, a top-level band that is itself only half written pushes down only the half that was written» — обобщена по пяти правилам там, где форма существует у двух; для трёх complexity-правил верхняя половина полосы это отказ, а не подача, и собственное измерение захода это знает («for all three complexity rules the top half band is **undeclared** — measured as a refusal probe», `population-gap.md:78-83`, 60 клеток с `live_exit=3`). Второе, и более дорогое для исполнителя: условие 4 в `unfold()` (`if ($group['warning'] === [] && $group['error'] === []) { continue; }`) стоит ПЕРВЫМ в теле цикла по группам, поэтому группа `''` complexity-трио пропускается целиком, ещё до того как цикл увидит написанный `threshold`. Новый кросс-путевой шаг P3, помещённый в этот цикл, для трёх правил из пяти — то есть ровно для семейства, о котором B1, — не исполнится ни разу. Подача для них вдобавок кросс-спеллинговая: одинокий `threshold` на пути `''` обязан стать парой `warning`/`error` на пути `callable`, и это НЕ тот вопрос, который закрывает вторая половина K1 (та про форму значения, не про наличие целевой группы). `00-overview.md` цитирует комментарий `LONE_THRESHOLD_SHAPE`, но как «форму, которую реестр не умеет объявить», а не как гейт, который пропустит новый шаг.
- **trigger**: воспроизводится при исполнении P3 — первая же реализация подачи по тексту P3 не тронет complexity-семейство
- **in_scope**: да (`01-contract.md:187-215`, `02-cure.md:135-162`)
- **anchor**: `docs/internal/plans/shorthand-scope/01-contract.md:196-198` (строки трёх complexity-правил), `:45-49` (симметрия половины полосы), `02-cure.md:141-150` (что меняет P3); `src/Analysis/Finding/RuleConfiguration/RuleThresholdKeyGroupRegistry.php:130-136,158-176`; `src/Analysis/Finding/RuleConfiguration/RuleOptionThresholdShorthand.php:110-114`
- **evidence**:
  ```php
  // RuleThresholdKeyGroupRegistry — путь '' трёх complexity-правил
  private const array LONE_THRESHOLD_SHAPE = ['warning' => [], 'error' => [], 'threshold' => ['threshold']];
  'complexity.ccn' => ['' => [[...self::LONE_THRESHOLD_SHAPE, 'form' => WholeNumber]], ...],
  ```
  ```
  # верхнеуровневые принимаемые ключи, снято с продукта
  ComplexityOptions: enabled, threshold, callable, class
  NpathComplexityOptions: enabled, threshold, callable, class
  CognitiveComplexityOptions: enabled, threshold, callable, class
  CboOptions: enabled, error, scope, threshold, warning, class, namespace
  # и прогон продукта на верхней половине полосы:
  complexity.ccn: {warning: 10}  -> exit 3  Option "warning" is not an option of rule "complexity.ccn"
  ```
- **verification**: confirmed
- **verification_note**: ключи сняты рефлексией `acceptedOptionKeys()` на нетронутом дереве, отказ воспроизведён прогоном `bin/qmx check` из каталога вне репозитория; условие 4 и его позиция в цикле прочитаны в коде. Измерение захода (`population-gap.md`, 60 клеток `in_declared_population=no`) — независимый второй свидетель того же факта, и он расходится именно с таблицей контракта, а не с продуктом.
- **fix_direction**: привести строки complexity-трио в таблице reach к тому, что объявляет реестр и принимает продукт (одинокий `threshold`), и в том же месте сказать, что верхняя половина полосы для этих трёх правил — отказ, а не подача; в P3 назвать условие 4 как гейт, который новый шаг обязан обойти, и назвать подачу для complexity кросс-спеллинговой (группа пути `''` и группа целевого уровня различаются не только формой значения, но и набором ключей), чтобы страж полноты после переопределения проверял это, а не только совпадение уровней.

---

### claude-r2-04

- **reviewer**: claude
- **severity**: MEDIUM
- **kind**: contract
- **domain**: tests
- **title**: Второй способ, которым стенд зеленеет вхолостую, и P0 его не закрывает: величину стороны выбирает НАБЛЮДЕНИЕ на том самом продукте, который проверяют, поэтому три прогона «по одному движению каждый» на части строк сравнивают три разных пробника
- **mechanism**: P0 называет одну вакуумность — обе стороны пары пишут одно значение — и лечит её различимыми величинами. Но `Stand::pairSide()` выбирает, писать каноническую величину или запасную, СРАВНЕНИЕМ с `omitted` на текущем продукте: каноническая берётся, только если объект от неё отличается от объекта при опущенном ключе, иначе перебираются запасные. Значит написанный документ — функция поведения продукта, а продукт в этом заходе меняется между прогонами. Конкретный случай, и он лежит ровно на предмете раунда: `complexity.npath: {enabled: true}` сегодня байт-в-байт равен опущенному ключу, поэтому сторона пишется запасной величиной `false`; после лечения это ровно B3 — верхний `enabled: true` включает класс-уровень, — и каноническая `true` перестаёт быть инертной, так что живой стенд напишет уже `true`. Четыре строки `complexity.npath | {callable,class}{,.enabled} × enabled` на прогоне №1 и на прогоне №3 будут про разные документы, и сравнение их чисел — не сравнение. Таблица «три числа, а не одно» этого не оговаривает, а P0 формулирует лечение как «distinguishable magnitudes per side, declared rather than computed» — если «declared rather than computed» понять буквально и убрать наблюдательный откат, вернётся дефект, ради которого `effect-magnitudes.tsv` и заведён (там измерено: «of the 117 pair sides that write the key `enabled`, 116 changed the object when the stand wrote an int at them and 0 did once the repaired generator wrote the canonical `true`»). То есть DoD P0 в его нынешней формулировке противоречит назначению файла, чей предмет он объявляет своим.
- **trigger**: воспроизводится в нормальной работе — на прогоне №3 после лечения, на четырёх строках npath
- **in_scope**: да (`02-cure.md:64-80`, `03-acceptance.md:11-40`, `:127-140`)
- **anchor**: `docs/internal/plans/shorthand-scope/02-cure.md:64-80` (P0), `03-acceptance.md:37-40` (DoD P0), `:127-140` (сетка трёх прогонов); `scripts/promise-effect/Stand.php:1363-1382` (`pairSide`); `promise-effect/effect-magnitudes.tsv` (шапка, абзац «HOW THE VALUE IS CHOSEN»)
- **evidence**:
  ```php
  // Stand::pairSide() — величину выбирает наблюдение на продукте под проверкой
  if ($canonical['object']->accepted() && $canonical['object']->text !== $omitted->text) {
      return [$candidates[0], $canonical, 'canonical magnitude'];
  }
  ```
  ```
  # NpathComplexityOptions::fromArray(), нетронутое дерево
  omitted (defaults) : {"callable":{...},"class":{"enabled":false,...}}
  {enabled: true}    : {"callable":{...},"class":{"enabled":false,...}}   <- байт-в-байт omitted
  {enabled: false}   : {"callable":{"enabled":false,...},...}             <- отличается
  # под Q5/B3 «{enabled: true}» обязан сравняться с «{class:{enabled:true}}», то есть перестать быть инертным
  ```
- **verification**: confirmed
- **verification_note**: инертность канонической `true` снята прямым вызовом продуктового `fromArray()`; «перестанет быть инертной после лечения» выведено из B3, который сам план объявляет ломающим изменением и подтверждает измерением E1/E2 — это единственная выводимая, а не наблюдённая часть.
- **fix_direction**: в DoD P0 развести два разных требования, которые сейчас слиты в одну фразу: (а) две стороны одной пары пишут разные значения; (б) выбор величины не зависит от продукта под проверкой. Для (б) решить явно, сохраняется ли наблюдательный откат — и если сохраняется, признать в таблице трёх прогонов, что на строках, где инертность меняется вместе с продуктом, числа прогонов не сравнимы построчно, и назвать эти строки поимённо (сегодня их четыре, все npath).

---

### claude-r2-05

- **reviewer**: claude
- **severity**: MEDIUM
- **kind**: contract
- **domain**: architecture
- **title**: claude-05 НЕ закрыта на `dd27e1c0`: решение «`scope` не подаётся на шве» сохраняет композицию, в которой побеждает НИЖНИЙ слой, а C3 теперь положительно утверждает, что этого не бывает ни в одной ориентации (в незакоммиченном дереве уже исправлено)
- **mechanism**: Раунд 1 нашёл, что `scope` нигде не размещён и что сегодняшняя развязка в `CboOptions::fromArray()` (`isset($config['scope']) && !isset($classConfig['scope'])`) — это «низ побеждает». Ревизия 2 разместила его и выбрала «оставить как есть»: Q3 говорит «That behaviour is kept exactly as it is, and P3 must not push `scope` down at the seam», таблица reach — «It is **not** pushed down at the seam», P4 — «preserved deliberately, with a test that fails if it is dropped». Но `fromArray()` видит уже слитый документ и про слои не знает: верхний слой, написавший верхнеуровневый `scope`, проигрывает нижнему слою, написавшему `class: {scope: …}`. C3 в той же ревизии утверждает обратное без оговорок — «Specificity never overrides layer priority, in either orientation», — и перечисляет `scope` в «What this answer does NOT change». То есть раунд 1 нашёл умолчание, а ревизия 2 заменила его положительно ложным утверждением: после лечения контракт будет описывать поведение, которого нет, и единственный ключ останется под тем самым режимом, который заход убирает.
- **trigger**: воспроизводится в нормальной работе — `--preset`/`qmx.yaml` с `class: {scope: …}` и верхнеуровневый `scope` в слое выше
- **in_scope**: да (`01-contract.md:64-71` C3, `:138-142` Q3, `:202-205` reach, `:335` «does NOT change»)
- **anchor**: `docs/internal/plans/shorthand-scope/01-contract.md:64-70` (C3), `:138-142` (Q3), `:202-205` (reach), `:335`; `src/Analysis/Evidence/Coupling/CboOptions.php:94-97`
- **evidence**:
  ```
  # <review>/raw/scope-layers.php — два слоя, как их собирает RuleOptionsFactory
  низкий слой (config file): {class: {scope: application}}
  высокий слой (CLI)       : {scope: all}
  -> class-level scope after merge = 'application'      # победил НИЖНИЙ слой
  ```
- **verification**: confirmed
- **verification_note**: прогон — прямой вызов продуктовых `RuleOptionThresholdShorthand::unfold()` и `CboOptions::fromArray()` через автозагрузчик репозитория, без единой правки дерева; порядок слоёв воспроизводит `RuleOptionsFactory::create()` (file → cli). ОТДЕЛЬНО: незакоммиченная правка рабочего дерева (06:20) эту находку уже закрывает — `scope` переводится на шов, C3 сохраняется, а старое поведение названо «C3 violated in the orientation nobody would notice». Находка остаётся в отчёте, потому что на ревьюируемом коммите она открыта и потому что правка не закоммичена.
- **fix_direction**: закоммитить уже сделанный в рабочем дереве выбор (подавать `scope` на шве и снять композицию из `fromArray()`) — и тогда внести разнос `scope` в файловый набор P4 как удаляемый, а не как сохраняемый с тестом, иначе P4 будет требовать двух взаимоисключающих вещей.

---

### claude-r2-06

- **reviewer**: claude
- **severity**: MEDIUM
- **kind**: contract
- **domain**: tests
- **title**: DoD P0 «различимые величины на сторону, объявленные, в файле, чей это предмет» недостижим для двух форм из восьми, и у `effect-magnitudes.tsv` нет селектора «сторона» — цена третьего селектора не названа
- **mechanism**: P0 объявлен «перед всеми остальными пакетами», и его DoD — единственный числовой рубеж, по которому судят прогон №1. Но «величина на сторону» в `effect-magnitudes.tsv` невыразима: файл объявляет ровно два селектора — `form` (запасная величина целой формы) и `option_leaf` (лист, чьи допустимые значения — перечисление), — и оба отвечают на вопрос «какое ВТОРОЕ значение написать вместо канонического», а не «какое значение написать ЭТОЙ стороне пары». Нужен третий вид селектора, и его цена (строка в файле, чтение в `Declarations`, ветка в `pairSide()`, стражи) нигде не посчитана, хотя цена нового значения `deeper-wins:` посчитана по пунктам. Плюс для двух форм третьего значения не существует: у `bool` их два, и одно из них — дефолт уровня; у листа `scope` перечисление `all`/`application`, где `all` и есть дефолт. Ровно эти листы дают шесть ячеек, которые остаются NOT OBSERVABLE и после P0 (измерено, claude-r2-01). DoD, сформулированный как достигаемое свойство, на этих шести ячейках не достигается никогда, и прогон №1 придётся судить не по нему.
- **trigger**: воспроизводится при исполнении P0 — первая же попытка объявить величину на сторону
- **in_scope**: да (`02-cure.md:64-80`, `03-acceptance.md:37-40`)
- **anchor**: `docs/internal/plans/shorthand-scope/02-cure.md:64-80`, `03-acceptance.md:37-40`; `promise-effect/effect-magnitudes.tsv` (абзац «TWO SELECTORS, ONE QUESTION»); `scripts/promise-effect/Stand.php:1467-1520`
- **evidence**:
  ```
  # effect-magnitudes.tsv — два селектора, оба про «второе значение ключа», не про сторону
  selector_kind  selector  yaml_write  why the canonical write cannot carry an effect here
  form           bool      false       `true` is the standing value of most `enabled` keys ...
  option_leaf    scope     application `ClassCboOptions::parseScope()` accepts `all` and `application` ...
  ```
  Остаток после P0 (прогон, см. claude-r2-01): шесть ячеек NOT OBSERVABLE —
  четыре `complexity.npath | *.enabled × enabled` (`bool`) и две
  `coupling.cbo | class.scope × scope` (перечисление).
- **verification**: confirmed
- **verification_note**: селекторы прочитаны в шапке файла и в `Stand::effectWritesFor()`/`writeForShape()`; остаток из шести ячеек измерен прогоном на замороженных наблюдениях, в двух вариантах (переписана верхняя сторона / переписана глубокая) с одинаковым результатом.
- **fix_direction**: переписать DoD P0 как два раздельных обязательства — «объявлено, где берётся вторая величина стороны» (с ценой нового вида селектора) и «список ячеек, где различимость недостижима, назван поимённо ДО прогона», — и оценивать прогон №1 по второму списку, а не по «все различимы».

---

### claude-r2-07

- **reviewer**: claude
- **severity**: LOW
- **kind**: point
- **domain**: reliability
- **title**: Заголовок «The 124 judged cells take» стоит над таблицей, суммирующейся в 128: четыре ячейки `refuse` в исходном измерении стоят ВНЕ ста двадцати четырёх
- **mechanism**: `03-acceptance.md` пишет «Of 465 axis-B cells, 128 are within the cure's radius … The 124 judged cells take:» и даёт таблицу `deeper-wins` 80 + `compose` 27 + `one-wins` 17 + `refuse` 4 = 128. Источник, из которого число перенесено, считает иначе: `axis-b-values.md` в шапке пишет «Assigned over the 124 judged movable cells: compose 27 · deeper-wins 80 · one-wins 17 (+4 `refuse` cells C4 leaves untouched)» — то есть 124 = 27+80+17, а четыре `refuse` стоят за скобкой. При переносе скобка втянута в таблицу, и заголовок перестал сходиться с телом. Пересчёт по самому файлу назначений подтверждает разбиение: 465 строк, 128 с `affected=yes`, 27/80/17/4.
- **trigger**: воспроизводится при чтении — число из этого раздела цитируется как рубеж приёмки
- **in_scope**: да (`03-acceptance.md:53-62`)
- **anchor**: `docs/internal/plans/shorthand-scope/03-acceptance.md:53-62`; `docs/internal/plans/shorthand-scope/measurement/axis-b-values.md:1-6`
- **evidence**:
  ```
  awk по measurement/axis-b-rows.tsv: всего 465, affected=yes 128
  compose 27 | deeper-wins 80 | one-wins 17 | refuse 4   (сумма 128)
  27 + 80 + 17 = 124        <- то, что измерение называет «judged»
  ```
- **verification**: confirmed
- **verification_note**: пересчитано по самому `axis-b-rows.tsv`, а не по прозе обоих документов.
- **fix_direction**: вынести четыре `refuse`-ячейки из таблицы 124 отдельной строкой, как это сделано в источнике, либо сменить заголовок на 128 — в обоих случаях так, чтобы сумма тела совпадала с заголовком.

---

## Статус находок раунда 1 — одним словом каждая

| id                                                                | статус                       | чем закрыта                                                                                                                                                                                                                                    |
| ----------------------------------------------------------------- | ---------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| claude-01 (`compose` неверен для 15 из 18)                        | закрыта                      | учёт переписан построчно (`axis-b-rows.tsv`, 465 строк), введено значение `deeper-wins:<key>` с ветвью и восемью контролями; §15 измерения отдельно фиксирует, что механизм claude-01 верен, а его следствие на реальных величинах недостижимо |
| claude-02 (18 cross-source строк не дойдут до классификатора)     | закрыта                      | 18 строк оставлены `DEFERRED`, настоящий блокер назван (`Stand::axisB()` same-source only, `PairRow` без ориентации), расширение оси B объявлено отдельным предметом                                                                           |
| claude-03 (таблица Q1 не различает семейства; межслойное лицо B1) | закрыта                      | в Q1 добавлена колонка правила, строки разведены по семействам, межслойное лицо B1 названо и вынесено в CHANGELOG и на `rules/complexity.md`                                                                                                   |
| claude-04 (верхний `enabled` включает класс npath)                | закрыта                      | стало B3, третьим ломающим изменением, с измерением E1/E2                                                                                                                                                                                      |
| claude-05 (`scope` не размещён; нарушает C3)                      | **НЕ закрыта** на `dd27e1c0` | см. claude-r2-05 (в незакоммиченном дереве уже исправлено)                                                                                                                                                                                     |
| claude-06 (нет запрета уничтожить существующий отказ)             | закрыта                      | K5 введена, у неё отдельный приёмочный кейс в DoD P3; остаток — форма «скалярный слот» всё ещё вне популяции, но кейс назван явно                                                                                                              |
| claude-07 (цена `one-wins:` завышена на несуществующей дыре)      | закрыта                      | §5 измерения перемеряла дыру, сузила до «суффикс, не совпавший ни с одним ключом», и превратила её в страж `assertKnownCoexistence`                                                                                                            |
| claude-08 (Q3: «один раз», а единиц три)                          | закрыта                      | Q3 переписан как три случая — полоса по полосе, `enabled` по ключу, остальное не заполняется вовсе                                                                                                                                             |
| claude-09 (популяция уже контракта)                               | **закрыта наполовину**       | `population-gap.tsv` добавил 137 клеток (T7 и B10–B12), но верхней формы «два спеллинга одной полосы» по-прежнему нет — это claude-r2-02, и она оказалась не косметикой                                                                        |
| claude-10 (K1 не говорит, форма какого пути)                      | закрыта                      | вторая половина K1 добавлена дословно про форму ЦЕЛЕВОГО пути                                                                                                                                                                                  |

## Coverage — что проверено и признано чистым

- **Значения в таблице назначений (вопрос 2 брифа).** Строки с неверным `coexistence` я не нашёл. Проверял не выборочно: 128 назначаемых ячеек сведены в 21 структурную форму по признакам (семейство × форма ключа A × её группа × форма ключа B × её группа), и каждая форма разобрана вручную против C1–C4 — включая обе ориентации (блок бывает и `key_a`, и `key_b`: три строки из двенадцати coupling-блочных), половину полосы на уровне (`class.error × threshold` — вторая половина берёт дефолт уровня, K4), `scope` (reach только `class`, поэтому `one-wins`, а не `deeper-wins`), и пары двух верхних ключей (4 `refuse` + 8 `compose`). Отдельно проверено, что для `min-afferent`/`min-class-count` верхнеуровневых ключей у продукта нет, поэтому строк, которые вторая половина C2 оставила бы без значения, в леджере не возникает.
- **15 строк NOT OBSERVABLE перечислены верно как перечень.** Пересчитано по `axis-b-rows.tsv`: ровно 15 ячеек с `post_cure_under_assigned = NOT OBSERVABLE`, и их состав совпадает с таблицей в `03-acceptance.md` построчно. Неверен не перечень, а его причина и его будущее под P0 — claude-r2-01.
- **Дубли документов (вторая половина P0).** Проверено пересчётом по леджеру: 19 троек `(rule, key_a, key_b)` в `same-source` несут по две строки (38 строк), из них 10 сегодня MISCOMPOSED — то есть «ten of axis B's defects are second copies» сходится. `PairRow::key()` включает `kind` намеренно, с докблоком; утверждение плана верно.
- **`axis-b-mechanisms.tsv` как знаменатель.** Пересчитано: 20+25+36+1 = 82 против живой оси 81 (`axis-b-rows.tsv`: 81 MISCOMPOSED из 465). Диагноз плана («M4 считает исчезнувшую ячейку, M2 поглощает npath-ячейку, которой её текст не описывает») воспроизводится.
- **Определение `deeper-wins:<key>` (вопрос 3 брифа) — верного вердикта не ломает ни одна форма пары из этой популяции.** Опасное место — регион как общий префикс с откатом на сегмент: откат делает регионом весь УРОВЕНЬ, даже когда глубокий ключ назвал один лист, а внутренний тест — строгое полистовое равенство. Это дало бы ложный красный на паре «глубокий ключ одной группы × верхний ключ ДРУГОЙ группы», где верхний законно заполняет незанятую группу того же уровня. Таких строк с назначением `deeper-wins` нет: функция назначения присуждает `deeper-wins` только когда группы совпадают или глубокая сторона — блок (а блок пишет все листы уровня, включая ту группу), и §2 измерения этот довод приводит явно. Восемь контролей покрывают обе стороны потери и обе двери чувствительности; положительный контроль NC8 в том же прогоне исключает сплошную зелень. Единственная тихая клетка названа самим измерением (`deeper-wins:<реальный ключ>` на `compose`-строке уходит в NOT OBSERVABLE, а не в красное). Незакоммиченная правка рабочего дерева добавляет седьмой недостающий пункт определения — «у глубокого ключа не должно быть эффекта ВНЕ своего региона», — который я вывел независимо и подтверждаю как верный.
- **P0 необходим.** Подтверждено кодом: `forms.tsv` фиксирует int → 7331, `effectWritesFor()` возвращает эту величину для каждого числового ключа независимо от стороны, а `Stand::pairSide()` переходит на запасную только когда каноническая неотличима от опущенного ключа — что на числовых ключах этих пяти правил не наступает. Расчёт §14 («33 зелёных на нелеченом продукте») проверен на разборе `cbo | class: × threshold` и сходится.
- **Полнота контракта по выросшей популяции (вопрос 4 брифа).** Прошёл по 757 клеткам (620 + 137) по обеим осям форм: документа, чей исход C1–C4 не определяют, среди них нет. Вторая половина C2 («подача только ДОБАВЛЯЕТ») действительно закрывает три новые формы B10–B12 и делает это без перечисления ключей. Единственный неопределённый документ лежит ВНЕ обеих популяций — верхнеуровневая смесь спеллингов, claude-r2-02.
- **Отказ верхнеуровневой смеси у complexity переживёт лечение.** Проверено прогоном: там `warning` не принимается на верхнем уровне вовсе, отказ даёт распознавание, а распознавание работает после подачи; подача ключ `warning` не тронет (условие 3 на пустом списке не срабатывает, но и подавать нечего). Дефект claude-r2-02 — только coupling-семейство, оба правила.
- **`layered-threshold` и пустой `declared-delta.tsv`** (проверено в раунде 1) — ревизией не тронуты, повторно не перепроверялись.
- **Пол, числа леджера 7/11/18, неблокирующая ось B, место лечения Q2, Q4, C4 как семантика, шесть мест сайта** — исключены брифом из повторной проверки, не перепроверялись.

## Refuted — гипотезы, которые я выдвинул и сам опроверг

- `R1 | P4 удаляет отказ верхнеуровневой смеси и у complexity-семейства тоже | опровергнуто прогоном: у complexity верхний `warning` не принимается вовсе, отказ даёт распознавание, а оно работает после подачи и переживёт её; дефект только coupling`
- `R2 | Регион `deeper-wins` (весь уровень) даёт ложный красный там, где верхний ключ законно заполняет другую группу того же уровня | опровергнуто: ни одна строка популяции с таким устройством не получает `deeper-wins` — функция назначения отдаёт их в `compose`, и §2 измерения приводит этот довод явно`
- `R3 | Верхнеуровневый `min_class_count`/`min_afferent` после лечения будет молча теряться (вторая половина C2 его не подаёт) | опровергнуто: этих ключей нет в `acceptedOptionKeys()` верхнего уровня ни у cbo, ни у instability, и пар с ними в леджере нет`
- `R4 | Восьми отрицательных контролей `deeper-wins` недостаточно — не хватает контроля на переполнение региона | опровергнуто наполовину: контроля действительно нет, но и достижимой формы нет; вместо находки это ушло в Coverage, а недостающий пункт определения уже добавлен незакоммиченной правкой`
- `R5 | Дублирующихся троек девятнадцать, а не тринадцать, значит докблок `PairRow::key()` врёт и счёт P0 неверен | опровергнуто по существу: счёт P0 («ten of axis B's defects are second copies») сходится точно; расхождение 19/13 — формулировка старого докблока, к этому заходу отношения не имеющая`

## Допущения

- Ревьюируется закоммиченный `dd27e1c0`. Рабочее дерево под ревью изменилось (три файла плана, 06:20–06:21, параллельный писатель) — это отмечено у каждой затронутой находки; своих правок в дерево я не вносил, все пробы в `mktemp -d` и в `<review>/raw/`.
- Исходы «после лечения» в claude-r2-01/02/03/04 выведены из текста контракта плюс измеренной сегодняшней базы. Симуляция P0 — переписывание одной стороны замороженного наблюдения на вторую величину; она моделирует свойство «стороны различимы», а не конкретную реализацию, которую P0 ещё выберет. Симуляция P4 — удаление ровно плоской ветки `CboOptions::fromArray()`, без шага подачи P3, потому что для этого документа подача по условию 3 не срабатывает.
- `composer promise-effect` не запускался. Числа оси B взяты из замороженных наблюдений продукта (`observations-before/raw.tsv`) через продуктовый `Classifier::pair()`.
- Severity проставлена по `finding-schema.md` с потолком по достижимости; claude-r2-03/05 ограничены MEDIUM правилом «`kind: contract` + `domain: architecture` без подтверждённого продуктового триггера», хотя оба подтверждены прогоном на уровне механизма.
