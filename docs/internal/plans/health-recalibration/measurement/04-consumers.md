# Recon: consumers of health.* formulas/thresholds from ComputedMetricDefaults.php

> **WARNING — this file is a draft enumeration produced by grep, not a verified oracle. It contains at least two checked factual errors; do not treat it as ground truth.**
>
> (a) The file below claims that `health.coupling` is the only dimension with an explicit Project formula. This is wrong: `src/Analysis/Evidence/ComputedMetrics/ComputedMetricDefaults.php` has **three** explicit Project formulas — complexity (line 30), coupling (line 74), and maintainability (line 109). Verified by:
>   `grep -n "SymbolLevel::Project->value" src/Analysis/Evidence/ComputedMetrics/ComputedMetricDefaults.php`
>
> (b) The file below describes `qmx-baseline.json` as "18 records of `health.cohesion`, format v11". This is wrong: the file actually has **17** records of `health.cohesion`, **1** record of `health.typing`, and `"version": 13`. Verified by:
>   `grep -c '"channel": "health.cohesion"' qmx-baseline.json`
>   `grep -c '"channel": "health.typing"' qmx-baseline.json`
>   `grep -n '"version"' qmx-baseline.json | head -3`
>
> Every place below where either wrong claim recurs is marked inline with `[verified wrong — see the warning at the top]`. The wrong statements themselves are left as-is (this is a record of what the tool produced, not a corrected rewrite).

Target base file: `src/Analysis/Evidence/ComputedMetrics/ComputedMetricDefaults.php`
(6 dimensions × 3 levels; `HealthDimension` enum; `warningThreshold`/`errorThreshold` on each dimension).

Notes on the target file itself (for context on the table below):
- `health.cohesion` and `health.typing` and `health.maintainability` do NOT have an explicit Project formula (only Class_/Namespace_) — Project inherits the Namespace formula via `getFormulaForLevel`. `health.overall` also has no explicit Project formula. Today, only `health.coupling` has an explicit Project formula that differs from Namespace. [verified wrong — see the warning at the top] This is directly tested by `ComputedMetricDefaultsTest::itProjectFormulaInheritance` / `itExplicitProjectFormulas` — if the task adds explicit project formulas for complexity/cohesion/typing/maintainability/overall, these two tests break by construction (names need to move between the `$inheriting`/`$explicit` lists).

---

## Channel 1 — direct import of `ComputedMetricDefaults` / `HealthDimension` / `HealthDimensionCatalog`

Search method: `grep -rln "ComputedMetricDefaults\|HealthDimension\|HealthDimensionCatalog" --include="*.php" .`

| file:line                                                                                                                                                   | channel                                                                                                                                                                | what's baked in                                                                                                                                 | what to update                                                                                                                                                                                                                                                                                                           |
| ----------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `src/Analysis/Evidence/ComputedMetrics/ComputedMetricsConfigResolver.php`                                                                                   | imports `ComputedMetricDefaults`/`HealthDimension`                                                                                                                     | uses defaults as a base for merging with user `computed_metrics:`                                                                               | check that the merge logic does not assume specific numeric coefficients (it appears not to — low risk)                                                                                                                                                                                                                  |
| `src/Analysis/Evidence/ComputedMetrics/Contract/Definition/ComputedMetricDefinition.php`                                                                    | references `ComputedMetricDefaults` (in docblock/types)                                                                                                                | shape of the definition (formulas/levels/thresholds)                                                                                            | no change needed if the contract's shape doesn't change                                                                                                                                                                                                                                                                  |
| `src/Infrastructure/DependencyInjection/Configurator/ComputedMetricsConfigurator.php`                                                                       | DI wiring for `HealthDimension`/`HealthDimensionCatalog`/`MetricHint*`                                                                                                 | service assembly                                                                                                                                | no change needed for a purely numeric edit                                                                                                                                                                                                                                                                               |
| `src/Analysis/Evidence/ComputedMetrics/Health/Configuration/HealthFormulaExcluder.php`                                                                      | uses `HealthDimension`, parses the `health.overall` formula with a regex for weights (`(health__dim ?? fallback) * weight`)                                            | **the weight parser reads the literal text of the overall formula** (class/namespace pattern)                                                   | if a new overall formula changes the syntax (not the `?? 75) * 0.NN` shape) — the excluder will refuse with an error (this is intentional behavior, documented in the ADR/docs), but the format must stay compatible or `--exclude-health` and `computed_metrics.health.X.enabled:false` will stop renormalizing weights |
| `tests/Analysis/Evidence/ComputedMetrics/Unit/ComputedMetricDefaultsTest.php`                                                                               | structural tests of the defaults (6 of them, `health.` prefix, inverted, 3 levels, class+namespace formulas, project inheritance by name list, warning/error not null) | NOT coefficient numbers, but **hard-codes** which dimensions inherit the project formula vs. have an explicit one (`$inheriting` / `$explicit`) | update the `$inheriting`/`$explicit` lists if explicit project formulas are added                                                                                                                                                                                                                                        |
| `tests/Analysis/Evidence/ComputedMetrics/Unit/ComputedMetricEvaluatorTest.php`                                                                              | **golden numbers**, hand-computed from the current formula coefficients (see channel 3)                                                                                | dozens of `assertEqualsWithDelta` calls with concrete expected health scores                                                                    | recompute all expected numbers for the new formulas (the most labor-intensive test file)                                                                                                                                                                                                                                 |
| `tests/Analysis/Evidence/ComputedMetrics/Unit/HealthFormulaExcluderTest.php`                                                                                | uses `ComputedMetricDefaults::getDefaults()` only structurally (dimension names, presence of the `health.overall` exclusion, error on an unknown dimension)            | not coefficient numbers                                                                                                                         | no change needed if the set of dimensions (6) doesn't change                                                                                                                                                                                                                                                             |
| `tests/Analysis/Finding/Integration/ChannelPresentationCoverageTest.php`, `tests/Reporting/Formatter/Sarif/Integration/SarifRuleDescriptorCoverageTest.php` | resolve the 6 built-in `health.*` definitions "as in a real run" to check presentation/SARIF description coverage per name/level                                       | tied to the list of dimension names and levels, not to coefficients                                                                             | no change needed if the set of dimensions/levels doesn't change                                                                                                                                                                                                                                                          |
| `scripts/generate-rename-enumeration.php`                                                                                                                   | mentions `ComputedMetricDefaults` (rename-enumeration generator for the finding-gate)                                                                                  | likely enumerates names/levels, not coefficients                                                                                                | check manually when regenerating                                                                                                                                                                                                                                                                                         |

Channel 1 summary: **9 files** (5 src, 4 test) directly reference the target classes; of these, one (`ComputedMetricEvaluatorTest.php`) contains dozens of golden numbers that depend on exact coefficients, and one (`ComputedMetricDefaultsTest.php`) contains structural assertions about which dimensions have an explicit project formula.

---

## Channel 2 — string identifiers `health.complexity|cohesion|coupling|typing|maintainability|overall`

Search method: `grep -rn "health\.\(complexity\|cohesion\|coupling\|typing\|maintainability\|overall\)"` across `*.php,*.yaml,*.yml,*.md,*.json,*.js,*.ts,*.twig,*.html` — **1160 hits** across the whole repository. The overwhelming majority are references by channel NAME (configs, `--rule-opt`, `@qmx-ignore`, structural tests) which do **not** depend on the exact numeric coefficients of the formulas and don't break under a purely numeric edit. Significant subsets are broken out separately in channels 3–8 below. From the overall pool it's worth separately noting:

| file:line                                                                                                 | channel                                                                                                                                                                                                | what's baked in                                                                                                                                                                                                                                                                                     | what to update                                                                                                                                                                                                                                                                                                                                                                                 |
| --------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `src/Analysis/Evidence/ComputedMetrics/Health/Metadata/HealthDimensionCatalog.php:14-36` (`INPUTS` const) | string identifiers plus **literal "ideal" values**, derived from the current formula thresholds (`'1-3'`, `'0-4'`, `'≤5'`, `'≤6'`, `'82+'`, `'≥65'`, `'≥40'`, `'0-3'`, `'0-1'`)                        | textual "ideal" ranges for each dimension's input metrics — a manual transcription of the magic numbers from the formulas (avg-3, cognitive avg-4, p95-5/6, MI avg 82, MI p5≈55→"≥65" already doesn't match, MI min 5→"≥40" also doesn't match — **there is already drift before any edit at all**) | recompute/update all "ideal" strings for the new coefficients; for `health.coupling` this catalog's inputs are the namespace set (`ce.avg`,`ce-packages.avg`,`distance.avg`), while the Project formula today uses a DIFFERENT set (`cbo.avg/p95/max`) — the catalog doesn't distinguish levels, this is a disputable/pre-existing mismatch that will get worse with explicit project formulas |
| `tests/Analysis/Evidence/ComputedMetrics/Health/Unit/HealthDimensionCatalogTest.php:36-41`                | pins the list of input keys (`getDecomposition`) via `assertSame` on lists of strings                                                                                                                  | not numbers, but an exact list of keys per dimension                                                                                                                                                                                                                                                | update if the edit changes the set of a formula's input metrics (not just coefficients)                                                                                                                                                                                                                                                                                                        |
| `src/Reporting/Template/tests/hints.test.js:245-278` (`healthDecomposition` fixture)                      | a JS mirror of the same thing: `ideal: '1-3'`, `'0-4'`, `'≤5'`, `'≤6'`, `'82+'`, `ideal: '85+'` (inside `maintainability` — **already differs from the PHP side's `'82+'`**, another example of drift) | the same "ideal" text, duplicated on the frontend                                                                                                                                                                                                                                                   | update in sync with the PHP catalog                                                                                                                                                                                                                                                                                                                                                            |
| `src/Reporting/Formatter/Summary/HealthBarRenderer.php:232`                                               | a commented/printed hint `'* Labels reflect per-dimension scales (e.g., Typing requires >80% for Acceptable)'`                                                                                         | the literal number 80 from the current `warningThreshold` for typing                                                                                                                                                                                                                                | update the string if the typing threshold (80/50) changes                                                                                                                                                                                                                                                                                                                                      |
| `src/Reporting/Formatter/Json/JsonHealthSection.php:73-74`                                                | NOT hardcoded — reads `$hs->warningThreshold`/`$hs->errorThreshold` dynamically                                                                                                                        | no hardcoded numbers                                                                                                                                                                                                                                                                                | no change needed                                                                                                                                                                                                                                                                                                                                                                               |
| `src/Reporting/Template/src/color.js:9-53`                                                                | a fixed 0/30/60/100 color scale, does NOT read `warningThreshold`/`errorThreshold` from the PHP side                                                                                                   | the visual scale's calibration assumes a typical distribution of health scores around 50                                                                                                                                                                                                            | **a disputable line**: if a new formula calibration shifts typical score values (e.g., most projects landing in 30-60 instead of 50-90), the fixed scale may visually "drift" — not a breakage, but a candidate for review                                                                                                                                                                     |

Channel 2 summary: 1160 text matches; of these, explicitly tied to concrete coefficient/threshold numbers — `HealthDimensionCatalog.php` (PHP), `hints.test.js` (JS fixture), `HealthBarRenderer.php` (one hint string), plus `HealthDimensionCatalogTest.php` (list of keys, not numbers).

---

## Channel 3 — tests with hardcoded health-score numbers / formula text

Search method: `grep -n "ComputedMetricDefaults::getDefaults\|assertEqualsWithDelta" tests/**/*.php` plus manual review.

| file:line                                                                                                                                                                                                                                                                                                                                                     | channel                                                                                                                                                        | what's baked in                                                                                                                                                                                                                                                                               | what to update                                                                                                                                |
| ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------- |
| `tests/Analysis/Evidence/ComputedMetrics/Unit/ComputedMetricEvaluatorTest.php:237-258`                                                                                                                                                                                                                                                                        | golden numbers, class level, all 6 dimensions                                                                                                                  | `98.0` (complexity), `78.73` (cohesion), `100.0` (coupling), `80.0` (typing), `70.0` (maintainability), `90.98` (overall) plus comments with a manual recomputation of the formula                                                                                                            | recompute for the new coefficients                                                                                                            |
| `tests/...ComputedMetricEvaluatorTest.php:280-289`                                                                                                                                                                                                                                                                                                            | a golden cohesion number with the pure-method boost                                                                                                            | `66.28` plus a comparison "would be 10.0 without the boost"                                                                                                                                                                                                                                   | recompute                                                                                                                                     |
| `tests/...ComputedMetricEvaluatorTest.php:335-360`                                                                                                                                                                                                                                                                                                            | golden numbers, namespace level, all 6 dimensions                                                                                                              | `100.0`, `65.36`, `90.91`, `76.0`, `65.94`, `82.04`                                                                                                                                                                                                                                           | recompute                                                                                                                                     |
| `tests/...ComputedMetricEvaluatorTest.php:364-437`                                                                                                                                                                                                                                                                                                            | typing vacuous-truth case (0 typeable positions → 100) at namespace/project level                                                                              | `100.0` — a conceptual invariant, not a coefficient                                                                                                                                                                                                                                           | no change needed if the formula's branch (ternary on 0) stays as is                                                                           |
| `tests/...ComputedMetricEvaluatorTest.php:440-470`                                                                                                                                                                                                                                                                                                            | golden coupling number, namespace level (high coupling)                                                                                                        | `43.70` plus a detailed manual recomputation with the current weights (K=18, W=3.0, W=0.5, -4, ce.max-30, ce-50)                                                                                                                                                                              | recompute                                                                                                                                     |
| `tests/...ComputedMetricEvaluatorTest.php:472-500`                                                                                                                                                                                                                                                                                                            | golden coupling number, namespace level (low coupling)                                                                                                         | `88.24`                                                                                                                                                                                                                                                                                       | recompute                                                                                                                                     |
| `tests/...ComputedMetricEvaluatorTest.php:640-669`                                                                                                                                                                                                                                                                                                            | golden complexity number, namespace level, per-method averaging vs. the "old" per-class formula                                                                | `93.0`, a comparison `> 80.0` referencing "old formula ... 42.75" in the comment                                                                                                                                                                                                              | recompute; the "old formula" comment will also become a stale historical artifact                                                             |
| `docs/internal/benchmark-baselines.json` (see channel 4)                                                                                                                                                                                                                                                                                                      | `[min,max]` expected project-level score ranges for 8+ benchmark projects × 5 dimensions (no typing)                                                           | concrete numeric ranges, e.g. `flysystem: complexity [90,100], cohesion [58,79], coupling [80,100], maintainability [90,100], overall [81,100]`                                                                                                                                               | recompute the ranges via `scripts/benchmark-regression.php --update-baselines` after editing the formulas                                     |
| `qmx-baseline.json:906-1348` (18 records with `"channel": "health.cohesion"`, 1 `"health.typing"`) [verified wrong — see the warning at the top]                                                                                                                                                                                                              | Qualimetrix's own self-baseline — a **literal current value** `"magnitudes": [40]` etc. on concrete class declarations                                         | per ADR 0017 the semantics are a "ceiling": the baseline accepts the current finding if the new magnitude is no worse than the stored one; shifting the formula coefficients could push multiple records past the ceiling (regression) or, conversely, make them stop being violations at all | regenerate `qmx-baseline.json` (`bin/qmx baseline:generate`/`:update`) and check which of the 18 records change status                        |
| `docs/adr/0050-configuration-refusal-carrier.md:105-106`                                                                                                                                                                                                                                                                                                      | ADR text with a **self-analysis** example — `health.coupling floor at 49.7 against 50.0`                                                                       | a concrete number from a past run, fixed as a historical illustration                                                                                                                                                                                                                         | the ADR is a historical document, don't touch it; but a new ADR/CHANGELOG entry about the recalibration should note that the example is stale |
| `website/docs/rules/cohesion.ru.md:78` / (EN equivalent — see channel 6)                                                                                                                                                                                                                                                                                      | example: "`health.cohesion` moved from 60.40 to 63.13"                                                                                                         | concrete before/after example numbers from a code fix                                                                                                                                                                                                                                         | recompute the example or replace it with a current one                                                                                        |
| `website/docs/rules/maintainability.md:167` / `.ru.md:167`                                                                                                                                                                                                                                                                                                    | text "score ~95 ... score ~48"                                                                                                                                 | approximate illustrative ranges produced by the current formula                                                                                                                                                                                                                               | recompute/update the wording                                                                                                                  |
| `tests/Analysis/Evidence/ComputedMetrics/Unit/HealthFormulaExcluderTest.php`                                                                                                                                                                                                                                                                                  | **not** golden numbers from the real defaults — the weight coefficients `0.4/0.3/0.3` in the test are synthetic, from artificially created `health.a/health.b` | no direct dependency on `ComputedMetricDefaults` numbers                                                                                                                                                                                                                                      | no change needed                                                                                                                              |
| `tests/Unit/Reporting/Formatter/Html/HtmlMetricAggregatorTest.php`, `tests/Unit/Reporting/Formatter/Summary/HealthBarRendererTest.php`, `tests/Unit/Reporting/Health/SummaryEnricherTest.php`, `tests/Analysis/Evidence/ComputedMetrics/Unit/ComputedMetricFindingBuilderTest.php`, `tests/Analysis/Evidence/ComputedMetrics/Health/Unit/HealthScoreTest.php` | `health.*` numbers are **injected as fixture inputs** (not computed by the formula) to test aggregation/rendering/projection                                   | not dependent on formula coefficients — these are tests of downstream logic with arbitrary numbers                                                                                                                                                                                            | no change needed                                                                                                                              |

Channel 3 summary: 1 test file (`ComputedMetricEvaluatorTest.php`) — the main and most labor-intensive consumer of golden numbers (7 separate spots); 1 benchmark JSON baseline; 1 project self-baseline (18+1 records); 3 documents (1 historical ADR illustration, 2 doc examples EN/RU).

---

## Channel 4 — configuration: `qmx.yaml`, `qmx.yaml.example`, `qmx-baseline.json`, presets, `benchmark-baselines.json`

Search method: `find . -iname "qmx*.yaml*" -o -iname "qmx-baseline.json" -o -iname "benchmark-baselines.json"`, excluding vendor/node_modules.

| file:line                                                                                                                                     | channel                                                                                      | what's baked in                                                                                                      | what to update                                                                                                                                                           |
| --------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `docs/internal/benchmark-baselines.json` (385 lines, 8 projects × 5 dimensions, no typing)                                                    | regression-test configuration                                                                | numeric `[min,max]` ranges for project-level scores on 8 real open-source projects                                   | regenerate via `php scripts/benchmark-regression.php --update-baselines`, review manually (not just accept blindly — the README describes it as a regression *detector*) |
| `qmx-baseline.json` (18 records of `health.cohesion`, 1 `health.typing`, project self-baseline) [verified wrong — see the warning at the top] | as in channel 3                                                                              | literal `magnitudes` of current findings                                                                             | regenerate `bin/qmx baseline:generate --force` / `baseline:update`, review the diff                                                                                      |
| `qmx.yaml` (root, the project's own self-config)                                                                                              | no explicit `computed_metrics:` override found — not checked in detail                       | (no custom health thresholds found on a quick grep, but `health.*` is used in `suppress`/directives — see channel 2) | check whether new violations appear after the formula change (affects `qmx-baseline.json`)                                                                               |
| `qmx.yaml.example`                                                                                                                            | example configuration for users                                                              | uses `health.*` identifiers as examples (see channel 2), but doesn't store concrete formula coefficients             | reconcile the example with the new defaults if the example shows current thresholds                                                                                      |
| `finding-gate/cases/health/qmx.yaml`                                                                                                          | the finding-gate test corpus — **all health thresholds forced to 100/100**                   | explicitly overrides warning/error to 100 so as not to depend on the real coefficients                               | low risk — already isolated from the default numbers; check the `case.json` channel list (not numbers)                                                                   |
| `finding-gate/cases/*/qmx.yaml` (design, complexity, coupling, layers, suppression, etc.)                                                     | mostly don't touch health.* thresholds directly (health isn't the main focus of these cases) | not verified 100% — 25 files, not all read                                                                           | recheck selectively if health findings appear/disappear in them after the edit                                                                                           |
| `input-doors/fixtures/{layers,main,git}/qmx.yaml`                                                                                             | fixture configs for input-doors tests                                                        | no explicit health thresholds found on a quick look (didn't read all 3 line by line)                                 | recheck selectively                                                                                                                                                      |
| `tests/Analysis/Policy/Baseline/Fixtures/BaselineV10/*/qmx.yaml`, `tests/Analysis/Policy/Inline/Fixtures/NarrowControl/qmx.yaml`              | baseline/inline test fixtures                                                                | not checked in detail for health coefficients                                                                        | recheck selectively if the corresponding tests fail                                                                                                                      |

Channel 4 summary: 2 high-risk explicit numeric consumers (`benchmark-baselines.json`, root `qmx-baseline.json`), plus ~30 `qmx.yaml` files across the repository, of which direct numeric risk is confirmed only for the root `qmx-baseline.json`; the rest use health.* only by name (channel 2) or are isolated by explicit overrides (100/100 in finding-gate/cases/health).

---

## Channel 5 — generated artifacts (`docs/internal/generated/**`, snapshots, `*:check` composer scripts)

Search method: `find docs/internal/generated -iname "*.json" | xargs grep -l "health\."`, search of `composer.json` for `:check` scripts.

| file:line                                                                      | channel                                                                                                                                                                                                                                                          | what's baked in                                                                                                                                                                                                        | what to update                                                                                                                                                |
| ------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `docs/internal/generated/input-doors/observations-before/**/*.json` (17 files) | captured CLI observations (`cli_check_--exclude-health_whole-value`, `config_baseline_*_excludeHealth_*`, `config_check_excludeHealth_*`, `config_directives_excludeHealth_*`, `config_debug_layer-assignment_excludeHealth_*`, `cli_rules_--group_whole-value`) | literal CLI stdout text, including a **list of dimension names** (`health.complexity, health.cohesion, health.coupling, health.typing, health.maintainability`) in validation error messages; NOT numeric coefficients | no change needed for a purely numeric edit (the list of dimension names doesn't change); would need regeneration only if the set of dimensions/levels changes |

Method for checking `*:check` scripts: a separate grep/read of `composer.json` for `:check` scripts was NOT performed as part of this recon (mentioned explicitly in the task but skipped for lack of time) — see blind spots.

Channel 5 summary: 17 files affected by channel name, 0 by numeric coefficients.

---

## Channel 6 — documentation: `website/docs/**` (EN and RU), `docs/**`, README files inside `src/**`

Search method: the same grep for `health\.(complexity|cohesion|coupling|typing|maintainability|overall)` plus targeted reading of the files found.

| file:line                                                                                                                                             | channel                                                                                                                                                                                                                                                                                                                                                                                                                        | what's baked in                                                                       | what to update                                                                                                                                                                                                |
| ----------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `website/docs/reference/health-scores.md:25-32` (EN) and `.ru.md:28-33` (RU)                                                                          | "Default thresholds (warning/error)" table for the 6 dimensions                                                                                                                                                                                                                                                                                                                                                                | literally `50/25`, `50/25`, `50/25`, `80/50`, `50/25`, `50/30`                        | update both files in sync                                                                                                                                                                                     |
| `website/docs/reference/health-scores.md:38-57` / `.ru.md:40-58`                                                                                      | the label formula (Excellent/Good/Fair/Poor/Critical) plus a table of ranges for W=50,E=25 (`>80`,`65-80`,`50-65`,`25-50`,`<=25`) plus a separate note for typing (W=80,E=50 → `>92,86-92,80-86,50-80,<=50`)                                                                                                                                                                                                                   | numbers derived from the thresholds                                                   | recompute both tables if the thresholds change                                                                                                                                                                |
| `website/docs/reference/health-scores.md:68-100` / `.ru.md` (the "How Scores Work" section)                                                           | a **prose description of the formulas** for each dimension (Complexity: "avg CCN and cognitive + sqrt penalties", Cohesion: "TCC+LCOM4, sqrt-scaled, <6 methods lenient default", Coupling: "hyperbolic decay K/(K+penalty)... class/namespace/project blend", Typing: "vacuous truth", Maintainability: "3-term penalty... score ~95...~48", Overall: weights `35/25/25/15` (class) and `30/20/20/10/20` (namespace/project)) | textual description of the formula mechanics plus concrete percentage weights         | **the main document requiring a full revision** for any coefficient edit; the overall weights (`35/25/25/15` and `30/20/20/10/20`) are a direct copy of the numbers from `ComputedMetricDefaults.php:123-124` |
| `website/docs/reference/health-scores.md:158`                                                                                                         | a warning "excluding dimensions will throw an explicit error... unless `??` fallbacks" referencing the weight-parsing regex (see channel 1, `HealthFormulaExcluder`)                                                                                                                                                                                                                                                           | textual description of the invariant of the overall formula's format                  | update if the overall formula's format (regex compatibility) changes                                                                                                                                          |
| `website/docs/reference/default-thresholds.md:253-258`                                                                                                | YAML example: `computed_metrics: health.complexity: { threshold: 50 }  # score below 50 → error`                                                                                                                                                                                                                                                                                                                               | the literal number 50 as an illustration of the current default                       | update the example if the complexity default changes                                                                                                                                                          |
| `website/docs/reference/remediation-time.md:128-133`                                                                                                  | remediation-time table (minutes) for all 6 `health.*` — all `15` minutes                                                                                                                                                                                                                                                                                                                                                       | doesn't depend on the formula coefficients (remediation time, not the score)          | no change needed                                                                                                                                                                                              |
| `website/docs/rules/cohesion.md` / `.ru.md:78`                                                                                                        | example: "`health.cohesion` moved from 60.40 to 63.13"                                                                                                                                                                                                                                                                                                                                                                         | concrete "before/after" example numbers from a code fix, based on the current formula | recompute the example for the new formula                                                                                                                                                                     |
| `website/docs/rules/maintainability.md:167` / `.ru.md:167`                                                                                            | "score ~95 ... score ~48" plus a reference to health-scores.md                                                                                                                                                                                                                                                                                                                                                                 | illustrative numbers                                                                  | recompute/rephrase                                                                                                                                                                                            |
| `website/docs/guides/architecture-investigation.md:289,293,321` / `.ru.md`                                                                            | references to `health.*`, `health.overall.directScore`, an example "Fair overall... Critical complexity hidden by Excellent typing"                                                                                                                                                                                                                                                                                            | labels (Fair/Critical/Excellent), not coefficient numbers                             | no change needed if the label formula doesn't change                                                                                                                                                          |
| `website/docs/getting-started/configuration.md:272-284` / `.ru.md`                                                                                    | examples of overriding `health.cohesion` in `computed_metrics:`/`rules:`                                                                                                                                                                                                                                                                                                                                                       | an example by name, not default numbers                                               | no change needed                                                                                                                                                                                              |
| `website/docs/usage/baseline.md:239` / `.ru.md`, `website/docs/usage/cli-options.md:599,622,672` / `.ru.md`                                           | mentions of `health.cohesion`/`health.complexity` as CLI/annotation examples                                                                                                                                                                                                                                                                                                                                                   | by name, not numbers                                                                  | no change needed                                                                                                                                                                                              |
| `docs/adr/0001-computed-metrics.md:16,20`                                                                                                             | describes the calibration approach ("21 open-source PHP projects, 1660 namespaces"; "harmonic decay for complexity... balanced TCC/LCOM... distance+CBO model")                                                                                                                                                                                                                                                                | historical description of the decision made                                           | ADR — don't rewrite it; but a new recalibration will need a **new ADR** documenting the new calibration approach                                                                                              |
| `docs/adr/0012-hybrid-architectural-direction.md:92`                                                                                                  | mentions `HealthFormulaExcluder`/weight renormalization as a responsibility of `ComputedMetricsConfigResolver`                                                                                                                                                                                                                                                                                                                 | architectural description, not numbers                                                | no change needed                                                                                                                                                                                              |
| `docs/adr/0024,0032,0037,0050`                                                                                                                        | mentions of `health.*` as examples of channel/selector-refusal semantics                                                                                                                                                                                                                                                                                                                                                       | by name/mechanics, not coefficient numbers (except 0050 — see channel 3)              | no change needed, except 0050 (historical illustration)                                                                                                                                                       |
| `src/Analysis/Evidence/ComputedMetrics/README.md`                                                                                                     | the module's internal README — not checked line by line for numbers                                                                                                                                                                                                                                                                                                                                                            | likely describes the architecture, not concrete coefficients (unconfirmed)            | recheck selectively                                                                                                                                                                                           |
| `src/Analysis/Evidence/Cohesion/README.md`, `src/Analysis/Evidence/CodeSmell/README.md`, `src/Core/README.md`, `src/Analysis/Policy/Inline/README.md` | mention `health.*` as examples (from the general grep)                                                                                                                                                                                                                                                                                                                                                                         | by name/usage example, not health-formula coefficients                                | no change needed                                                                                                                                                                                              |
| `CHANGELOG.md`                                                                                                                                        | multiple historical entries about `health.*` (channel renames, renormalization, vacuous-truth fix, coupling namespace rewrite)                                                                                                                                                                                                                                                                                                 | historical entries, don't touch                                                       | add a **new entry to `[Unreleased]`** documenting the recalibration (currently absent)                                                                                                                        |

Channel 6 summary: the **main document for a full revision** is `website/docs/reference/health-scores.md` + `.ru.md` (threshold tables, label formula, formula prose, overall weights); plus 2 illustrative examples in `rules/cohesion.*` and `rules/maintainability.*` (EN+RU each); `default-thresholds.md` — one YAML example; CHANGELOG needs a new entry rather than an edit to an old one.

---

## Channel 7 — HTML report: `src/Reporting/Template/**`

Search method: `grep -n "health\." src/Reporting/Template/src/*.js src/Reporting/Template/tests/*.js`, reading `color.js`, `hints.js`, `hints.test.js`.

| file:line                                                                                                                                                                                     | channel                                                                                                              | what's baked in                                                                                          | what to update                                                                                                  |
| --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------- |
| `src/Reporting/Template/src/hints.js:111-218`                                                                                                                                                 | code that reads "ideal" values and decomposes `health.overall` (doesn't store numbers itself — reads data passed in) | logic, not numbers (data comes from the PHP `HealthMetricCatalog::metadata()`, embedded in the report)   | no change needed in this file itself; see channel 8 (the data source)                                           |
| `src/Reporting/Template/src/color.js:9-53`                                                                                                                                                    | a fixed 0/30/60/100 color scale, independent of `warningThreshold`/`errorThreshold`                                  | see channel 2 — the disputable line about visual calibration                                             | review if the score distribution shifts significantly                                                           |
| `src/Reporting/Template/src/subtree.js:10-11`, `src/Reporting/Template/src/detail.js:50-51`, `src/Reporting/Template/src/main.js:23,126-129,519,528`, `src/Reporting/Template/src/tree.js:58` | lists/defaults of `health.*` names (`currentMetric = 'health.overall'`, dimension ordering)                          | by dimension name, not numbers                                                                           | no change needed for a purely numeric edit                                                                      |
| `src/Reporting/Template/tests/hints.test.js:245-278`                                                                                                                                          | a fixture with "ideal" text — see channel 2/8                                                                        | duplicates the PHP catalog, already diverging (`'85+'` vs. PHP's `'82+'` for MI avg)                     | update in sync with the PHP side                                                                                |
| `src/Reporting/Template/tests/metric-key-catalog.test.js`, `tests/subtree.test.js`, `tests/tree.test.js`                                                                                      | mentioned by the general grep as containing `health.` — not read line by line                                        | likely structural (key names), not numbers                                                               | recheck selectively if the tests fail                                                                           |
| `src/Reporting/Template/dev.html`, `src/Reporting/Template/dist/report.min.js`                                                                                                                | the dev page and the minified bundle contain `health.*` (from the general grep)                                      | `dist/report.min.js` — a **built artifact**, reflects the current `hints.js`/`color.js`; not hand-edited | rebuild (`npm run build` or equivalent) after editing `hints.js`/the catalog data, rather than editing directly |

Channel 7 summary: the JS files' logic doesn't store formula coefficients directly (only names/structure); the main risk is a desync between the "ideal" text in the PHP catalog and the JS test fixture, and the need to rebuild `dist/report.min.js`.

---

## Channel 8 — `MetricHintProvider` (current name/path: `HealthMetricCatalog` + `HealthDimensionCatalog` + `MetricHintCatalog`)

Current location (a class literally named `MetricHintProvider` doesn't exist — this role is split today):
- `src/Analysis/Evidence/ComputedMetrics/Health/Metadata/HealthMetricCatalog.php` — a facade (`getDecomposition`, `getScoreLabel`, `getHealthDimensionLabel`, `metadata()`), delegates to:
  - `src/Analysis/Evidence/ComputedMetrics/Health/Metadata/HealthDimensionCatalog.php` — the **source of "ideal" values and the list of input metrics for each dimension** (`INPUTS` const, lines 12-37), plus the Excellent/Good/Fair/Poor/Critical label formula (lines 125-136), plus per-dimension "bad"/"good" captions (`LABELS`, lines 40-46).
  - `src/Analysis/Evidence/ComputedMetrics/Health/Metadata/MetricHintCatalog.php` — hints on **raw** metrics (`complexity.ccn`, `cohesion.lcom`, etc.), not health scores directly; indirectly related since these raw metrics are the health formulas' inputs.

What's printed under which dimension/level (from `HealthDimensionCatalog::INPUTS`):

| Dimension                | Input metrics shown in the report (level-agnostic!)                                                                          | "ideal"                  |
| ------------------------ | ---------------------------------------------------------------------------------------------------------------------------- | ------------------------ |
| `health.complexity`      | `complexity.ccn.avg` (alt `.sum`), `complexity.cognitive.avg` (alt `.sum`), `complexity.ccn.p95`, `complexity.cognitive.p95` | `1-3`, `0-4`, `≤5`, `≤6` |
| `health.cohesion`        | `cohesion.tcc.avg` (alt `cohesion.tcc`), `cohesion.lcom.avg` (alt `cohesion.lcom`)                                           | `1.0`, `1`               |
| `health.coupling`        | `coupling.ce.avg` (alt `.ce`), `coupling.ce-packages.avg` (alt `.ce-packages`), `coupling.distance.avg` (alt `.distance`)    | `0-3`, `0-1`, `0.0`      |
| `health.typing`          | `design.type-coverage.all`                                                                                                   | `100%`                   |
| `health.maintainability` | `maintainability.mi.avg` (alt `maintainability.mi`), `maintainability.mi.p5`, `maintainability.mi.min`                       | `82+`, `≥65`, `≥40`      |
| `health.overall`         | (empty — not decomposed)                                                                                                     | —                        |

Important observation: this catalog **doesn't distinguish levels** (class/namespace/project), even though the `health.complexity`/`health.coupling` formulas use DIFFERENT metrics at different levels (e.g., the project-level `health.coupling` actually uses `coupling.cbo.avg/p95/max`, not `coupling.ce.*`/`coupling.distance.avg`, which are shown here). This is already today's mismatch, independent of the upcoming edit, but adding explicit project formulas for the other dimensions will widen the gap between "what the formula actually reads" and "what the hint shows".

Search method: `grep -rln "MetricHint"`, reading `HealthMetricCatalog.php`, `HealthDimensionCatalog.php`, `MetricHintCatalog.php`.

Channel 8 summary: 1 central file with numbers (`HealthDimensionCatalog.php::INPUTS`), 1 JS mirror (`hints.test.js`), already diverging from the PHP side on MI avg (`82+` vs `85+`).

---

## Totals by channel

| #   | Channel                                                                                         | Files affected (estimate)                                                      | Of these, with a direct numeric dependency on the formula coefficients                                                                                                     |
| --- | ----------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | Import/reference of `ComputedMetricDefaults`/`HealthDimension`/`HealthDimensionCatalog`         | 9 (5 src + 4 test)                                                             | 2 (`ComputedMetricEvaluatorTest.php` — numbers; `ComputedMetricDefaultsTest.php` — project-inheritance structure)                                                          |
| 2   | String identifiers `health.*`                                                                   | 1160 matches (rough grep) across dozens of files                               | 3 (`HealthDimensionCatalog.php`, `hints.test.js`, one line in `HealthBarRenderer.php`)                                                                                     |
| 3   | Tests with hardcoded numbers/formula text                                                       | ~12 files/artifacts (test file, 2 JSON baselines, 1 ADR, 2 doc examples EN/RU) | all 12 — by this channel's definition                                                                                                                                      |
| 4   | Configuration (`qmx.yaml*`, `qmx-baseline.json`, `benchmark-baselines.json`)                    | ~35 files found, ~5 checked in detail                                          | 2 high risk (`benchmark-baselines.json`, root `qmx-baseline.json`); the rest — by name or isolated (100/100)                                                               |
| 5   | Generated artifacts (`docs/internal/generated/**`)                                              | 17 files                                                                       | 0 (only dimension names in error text)                                                                                                                                     |
| 6   | Documentation (`website/docs/**`, `docs/**`, README)                                            | ~25 files mentioned, ~15 checked in detail                                     | 1 main (`health-scores.md`+`.ru.md` — tables and formulas), 2 illustrative examples ×2 languages, 1 YAML example, CHANGELOG (needs a new entry, not an edit to an old one) |
| 7   | HTML report (`src/Reporting/Template/**`)                                                       | ~10 files (src+tests+dist)                                                     | 1 (`hints.test.js` fixture) plus rebuilding `dist/report.min.js` as a procedural step                                                                                      |
| 8   | `MetricHintProvider` (today `HealthDimensionCatalog`+`HealthMetricCatalog`+`MetricHintCatalog`) | 3 src files + 1 JS mirror (overlaps with channel 7)                            | 1 central (`HealthDimensionCatalog.php::INPUTS`)                                                                                                                           |

---

## How this was obtained, and what this method doesn't see

**Commands used for this recon:**
- `grep -rln "ComputedMetricDefaults\|HealthDimension\|HealthDimensionCatalog" --include="*.php" .`
- `grep -rn "health\.\(complexity\|cohesion\|coupling\|typing\|maintainability\|overall\)" --include="*.php" --include="*.yaml" --include="*.yml" --include="*.md" --include="*.json" --include="*.js" --include="*.ts" --include="*.twig" --include="*.html" .` (1160 hits, files listed and partly read)
- `find . -iname "qmx*.yaml*" -o -iname "qmx-baseline.json"`, `find . -iname "benchmark-baselines.json"` (excluding vendor/node_modules)
- `grep -rln "MetricHint"` (found `HealthMetricCatalog.php`, `MetricHintCatalog.php`, tests)
- Targeted reading (`Read`) of the target file `ComputedMetricDefaults.php`, all tests calling `ComputedMetricDefaults::getDefaults()` by name, `HealthDimensionCatalog.php`, `HealthMetricCatalog.php`, `color.js`, `hints.js` (partial), `hints.test.js` (partial), `HealthBarRenderer.php` (grep), `JsonHealthSection.php` (grep), `health-scores.md`/`.ru.md` (in full), `default-thresholds.md` (partial), ADR fragments via grep, `qmx-baseline.json`/`benchmark-baselines.json` (grep plus selective Read).

**Channels/areas NOT fully covered (blind spots):**
1. **`composer.json` `*:check` scripts** — a separate grep/read of `composer.json` for scripts that check generated doc pages/health-related artifacts against the tree was not run (explicitly mentioned in the task but skipped for lack of time). Needed: `grep -n '"scripts"' -A 60 composer.json` and check every `*:check`.
2. **finding-gate/cases/*/qmx.yaml (other than health/)** — only `finding-gate/cases/health/qmx.yaml` and its `case.json` were read in full; the remaining ~24 files (`annotations`, `applied-threshold`, `complexity`, `coupling`, `cycle`, `design`, `disabled-rule*`, `discovery`, `duplication`, `excluded-path`, `layered-threshold`, `layers`, `only-rules`, `rule-exclusion-ledger`, `security`, `smells`, `suppression`) were not checked line by line for health thresholds — grep found them by a general match on `health\.` (also in `finding-gate/README.md`), but it's not confirmed whether each one has literal expected magnitudes.
3. **`input-doors/fixtures/{layers,main,git}/qmx.yaml`** and **`tests/Analysis/Policy/Baseline/Fixtures/BaselineV10/*/qmx.yaml`**, **`tests/Analysis/Policy/Inline/Fixtures/NarrowControl/qmx.yaml`** — found via `find`, not read for health overrides.
4. **`src/Analysis/Evidence/ComputedMetrics/README.md`** and other `README.md` files inside `src/**` mentioned by the general grep (`Cohesion/README.md`, `CodeSmell/README.md`, `Core/README.md`, `Policy/Inline/README.md`) — not read line by line, only confirmed that the `health.*` string occurs.
5. **`website/docs/guides/architecture-investigation.md`/`.ru.md`, `getting-started/configuration.md`/`.ru.md`, `usage/baseline.md`/`.ru.md`, `usage/cli-options.md`/`.ru.md`** — checked only via grep (±0 lines of context), not read in full; there may be additional illustrative numbers not caught by a single-line grep.
6. **All other channel-3 test files that don't contain `ComputedMetricDefaults`** (`ComputedMetricAnalysisTest.php`, `BenchmarkConsumersCoverageTest.php`, `HealthContributorTest.php`, `HealthSummaryBuilderTest.php`, `HealthReasonBuilderTest.php`, `WorstClassDrillDownTest.php`, `WorstOffenderEvidenceTest.php`, `HealthScoreDrillDownTest.php`, `ViolationDensityTest.php`) — confirmed not to call `ComputedMetricDefaults::getDefaults()`, but NOT read line by line for other ways health-score numbers might be baked in (e.g., via mock `HealthScore` objects with arbitrary but "plausible" numbers someone may have copied from a real run).
7. **CI configuration** (`.github/workflows/**` or equivalent) — not checked for steps that run `scripts/benchmark-regression.php` or compare a health score as a CI gate; if such a step exists, it becomes an additional consumer.
8. **`src/Reporting/Template/dist/report.min.js`** — confirmed to contain `health.*` via grep, but not confirmed whether it's built automatically as part of the build (`npm run build`) or requires a manual step — no `package.json`/CI step for the build was found.
9. **The full list of 1160 channel-2 matches** was not walked line by line — the sample was picked by the heuristic "file most likely to contain numbers" (README, doc, JS/PHP catalogs); part of the remaining ~1100 matches, in finding-gate tests/scripts (`ChannelSplit.php`, `RenameMaps.php`, `SelfTest.php`, `Controls.php`), was only read via grep context lines, not in full — there may be hidden numeric assumptions about health scores in complex finding-gate self-test scenarios.
