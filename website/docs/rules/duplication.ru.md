# Правила дублирования кода

Дублирование кода -- один из самых распространённых источников технического долга. Когда одна и та же логика существует в нескольких местах, каждое исправление бага или добавление функциональности должно быть применено ко всем копиям -- и неизбежно какие-то копии будут пропущены. Эти правила обнаруживают структурно идентичные блоки кода по всей кодовой базе с помощью анализа потока токенов.

---

## Дублирование кода (Code Duplication)

**Идентификатор правила:** `duplication.clone`

<!-- llms:skip-begin -->
### Что измеряет

Обнаруживает дублированные блоки кода между файлами с использованием хеширования потока токенов. Алгоритм работает в три этапа:

1. **Токенизация** -- PHP-код разбирается в поток токенов.
2. **Нормализация** -- токены трансформируются для игнорирования косметических различий: переменные заменяются на `$_`, строковые литералы на `'_'`, числа на `0`, пробелы и комментарии удаляются. Имена функций, методов и классов сохраняются, поэтому помечается только **структурно идентичный** код с разными именами переменных.
3. **Обнаружение** -- дублированные последовательности находятся с помощью скользящего хеша (алгоритм Рабина-Карпа). Блоки короче минимальных пороговых значений игнорируются.

Представьте это как сравнение рецептов: если два рецепта содержат одни и те же шаги в том же порядке, но с разными названиями ингредиентов -- это дубликаты.

<!-- llms:skip-end -->

<!-- llms:skip-begin -->
### Пороговые значения

| Значение                  | Серьёзность | Значение                                                         |
| ------------------------- | ----------- | ---------------------------------------------------------------- |
| < 50 дублированных строк  | Warning     | Заметное дублирование, стоит подумать об извлечении общей логики |
| >= 50 дублированных строк | Error       | Значительное дублирование, рефакторинг настоятельно рекомендован |

Минимальный размер блока (настраивается):

| Опция        | По умолчанию | Значение                                         |
| ------------ | ------------ | ------------------------------------------------ |
| `min_lines`  | 5            | Минимальное количество строк для проверки блока  |
| `min_tokens` | 70           | Минимальное количество токенов для пометки блока |
<!-- llms:skip-end -->

<!-- llms:skip-begin -->
### Пример

Эти два метода имеют идентичную структуру, но разные имена переменных -- правило помечает их как дубликаты:

```php
// В OrderService.php
public function calculateOrderTotal(Order $order): float
{
    $total = 0.0;
    foreach ($order->getItems() as $item) {
        $price = $item->getPrice();
        $quantity = $item->getQuantity();
        $subtotal = $price * $quantity;
        if ($item->hasDiscount()) {
            $subtotal *= (1 - $item->getDiscount());
        }
        $total += $subtotal;
    }
    return $total;
}

// В InvoiceService.php -- структурно идентичный
public function calculateInvoiceAmount(Invoice $invoice): float
{
    $amount = 0.0;
    foreach ($invoice->getLineItems() as $lineItem) {
        $rate = $lineItem->getPrice();
        $qty = $lineItem->getQuantity();
        $lineTotal = $rate * $qty;
        if ($lineItem->hasDiscount()) {
            $lineTotal *= (1 - $lineItem->getDiscount());
        }
        $amount += $lineTotal;
    }
    return $amount;
}
```

После нормализации оба метода дают одинаковую последовательность токенов -- переменные заменяются на `$_`, но имена методов/классов вроде `getPrice`, `getQuantity`, `hasDiscount` сохраняются.

<!-- llms:skip-end -->

<!-- llms:skip-begin -->
### Как исправить

1. **Извлечение метода (Extract Method)** -- перенесите общую логику в общий метод или сервис:

    ```php
    final class PriceCalculator
    {
        /** @param iterable<PriceableItem> $items */
        public function calculateTotal(iterable $items): float
        {
            $total = 0.0;
            foreach ($items as $item) {
                $subtotal = $item->getPrice() * $item->getQuantity();
                if ($item->hasDiscount()) {
                    $subtotal *= (1 - $item->getDiscount());
                }
                $total += $subtotal;
            }
            return $total;
        }
    }
    ```

2. **Паттерн Стратегия** -- когда дублированные блоки отличаются в нескольких шагах, вынесите варьирующиеся части в объекты-стратегии.

3. **Шаблонный метод (Template Method)** -- когда общая структура одинакова, но подклассы различаются в конкретных шагах, используйте абстрактный базовый класс с шаблонными методами.

<!-- llms:skip-end -->

<!-- llms:skip-begin -->
### Детали реализации

Qualimetrix использует алгоритм **скользящего хеша Рабина-Карпа** для эффективного обнаружения. Ключевой шаг -- нормализация, которая позволяет находить "почти-дубликаты", отличающиеся только именами переменных, строковыми значениями или числовыми константами. Этот подход аналогичен инструментам PMD CPD и Simian.

Поскольку имена функций/методов/классов сохраняются при нормализации, детектор **не** помечает два метода, вызывающих совершенно разные API, даже если их структура потока управления идентична.

Реализация принадлежит модулю `Analysis.Evidence.Duplication`, который
объединяет обнаружение, результат одного запуска и правило. Оркестратор запуска
вызывает модуль через узкий контракт reset/inspect; это изменение владения не
меняет идентификатор правила, опции, алгоритм или вывод.

!!! info "Deviation from original spec"
    Группа дубликатов использует как семантическое occurrence полный SHA-256
    всей нормализованной совпавшей последовательности токенов вместе с числом
    токенов. Subject нарушения — проект, и все копии блока разделяют эту
    идентичность; строки, порядок обнаружения и порядок связанных копий влияют
    только на представление. Поэтому добавление лексически более ранней копии
    не меняет существующую запись baseline или fingerprint форматтера, а
    добавляет один член в группу, которую ограничивает запись.

!!! warning "Инлайновый `@qmx-ignore` не подавляет этот канал"
    Все копии блока — один и тот же долг уровня проекта, поэтому ни одной
    инлайновой директиве не позволено его подавить: `@qmx-ignore`
    привязывается к объявлению, на котором написан, а проект никогда им не
    является; `@qmx-ignore-file` и `@qmx-ignore-next-line` подавили бы одну
    копию, рядом с которой написаны, а все остальные копии продолжали бы
    сообщать блок — и копия, вставленная вместе с такой директивой, прошла бы
    baseline незамеченной. Все три формы отклоняются
    (`annotation.unresolved-directive`) там, где написаны. Вместо этого
    отключите правило — `disabled_rules: [duplication.clone]` в
    конфигурации или `--disable-rule=duplication.clone` — либо примите блок
    со всеми его копиями в baseline.

!!! info "Константные массивы и массивы-свойства исключены всегда"
    Дублированный блок, который **целиком** лежит внутри объявления `const` или инициализатора статического/обычного свойства-массива, никогда не сообщается. Строки таблицы соответствий (например, `'key' => ['a' => ..., 'b' => ...]`, повторённые с разными значениями) после нормализации дают одинаковую последовательность токенов, но совет "вынести общий метод" не применим к таблице данных -- повторение одной и той же формы полей в разных строках и есть нормальная, правильная форма такой таблицы. Блок, пересекающий границу между объявлением данных и исполняемым кодом (или целиком лежащий в теле метода), по-прежнему сообщается. Это подавление безусловно и не может быть отключено.

!!! info "Каждая копия блока — своя находка"
    Каждая копия дублированного блока сообщается находкой, расположенной на этой копии, сколько бы копий ни было, — верхнего предела на число копий нет. Сообщение называет число вхождений и до десяти других копий, а если их больше — добавляет `and N more`; связанные расположения находки — это копии, названные в её сообщении. Все копии блока разделяют одну идентичность, поэтому запись baseline ограничивает число копий: новая копия принятого блока — нарушение, а `--report=git:*` сообщает её в том файле, куда её вставили. Если копии совпадают на разную длину, сообщается каждое наибольшее совпадающее множество: две копии, совпадающие на 40 строк, и третья, совпадающая с ними только на первых 10, дают по находке на каждую из двух 40-строчных копий и по находке на каждую из трёх копий по 10 строкам.

!!! info "Копии внутри одного файла"
    Два вхождения в одном файле, у которых есть общая строка, — это одна повторяющаяся структура, совпавшая сама с собой со сдвигом, а не две копии, и остаётся только первое. Вхождения, которые лишь соприкасаются — одно заканчивается на строке перед началом другого, — это две копии, и они сообщаются.

!!! tip "Интеграция с IDE"
    При использовании вывода в формате SARIF (`--format=sarif`) дублированные копии связываются через `relatedLocations`. Это означает, что копии дубликатов отображаются как **кликабельные перекрёстные ссылки** в VS Code (расширение SARIF Viewer) и JetBrains IDE, что упрощает переход от каждой копии к названным в ней остальным.
<!-- llms:skip-end -->

### Превью содержимого

Находки дублирования включают превью содержимого дублированного блока. Это помогает быстро определить, какой именно код дублирован, без перехода к файлу:

```
Duplicated code block (16 lines, 2 occurrences): "$total = 0.0; foreach ($order->getItems() as $item) { $price =..." — also at src/Service/InvoiceService.php:15-30
```

Превью — исходный текст первой копии: до трёх из её первых десяти строк без пустых строк и строк из одних скобок, с схлопнутыми пробелами, обрезанный примерно до 80 символов.

### Конфигурация

```yaml
# qmx.yaml
rules:
  duplication.clone:
    enabled: true
    min_lines: 5
    min_tokens: 70
    warning: 5    # дублированных строк
    error: 50
```

```bash
# Увеличить минимальный порог токенов для снижения шума
bin/qmx check src/ --rule-opt="duplication.clone:min_tokens=100"

# Увеличить минимальное количество строк
bin/qmx check src/ --rule-opt="duplication.clone:min_lines=10"

bin/qmx check src/ --rule-opt="duplication.clone:warning=10"
bin/qmx check src/ --rule-opt="duplication.clone:error=60"
```

Простой порог вместо раздельных уровней warning/error
(`threshold` нельзя сочетать с `warning` или `error` — смешение считается
ошибкой конфигурации, и прогон останавливается с кодом 3):

```yaml
rules:
  duplication.clone:
    threshold: 50   # warning=50, error=50 → все нарушения становятся ошибками
```

```bash
bin/qmx check src/ --rule-opt="duplication.clone:threshold=50"
```

Также можно полностью отключить правило:

```bash
bin/qmx check src/ --disable-rule=duplication.clone
```

!!! note "Потребление памяти"
    Обнаружение дубликатов использует алгоритм скользящего хеша Рабина-Карпа, который требует хранения нормализованных токенов всех файлов с совпавшими хешами в памяти одновременно. На больших кодовых базах (500+ файлов) это может потреблять значительный объём памяти. Отключение правила — `--disable-rule=duplication.clone` или `enabled: false` в разделе `duplication.clone` конфигурации — полностью пропускает фазу обнаружения и освобождает память.
