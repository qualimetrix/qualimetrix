# Channels verified at their emission point to deliberately declare no
# baseline support (ADR 0017). Their absence from
# declared.txt is not drift — a channel that declares nothing is legitimately
# "not baselineable", not an oversight.
#
# The file is currently empty of entries, and that is the point: the only
# family that ever needed one was annotation.*, which had no rule class to
# declare it on. It has one now, and "not acceptable as debt" is stated by a
# declaration rather than by absence.
#
# Format (one channel per line):
#   <ruleName>#<violationCode> -- <reason>
#
# Used by tests/Analysis/Finding/Integration/ChannelCoverageTest.php to distinguish
# a deliberate exclusion from an undeclared channel that should have been
# covered by this package's own corpus.

# (Currently empty. The annotation.* family used to live here because it was
# emitted straight from the pipeline with no rule class to declare it on; it
# now has one, so its channels are declared like any other.)
