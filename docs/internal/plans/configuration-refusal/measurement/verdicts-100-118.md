# Пакет C — позиции 100–118 (M7, M10, #110 из M13). Дерево 1513bf67

Контроли пересняты, различимы: BASE n=5 41fa12dcb9 | DEFAULT n=3 fb57cbfbaa |
NOCCN n=2 d7ee53775e | EMPTY n=0 d751713988. Неиспорченный baseline (v13, 5 записей)
гасит находки полностью — digest уходит в EMPTY. Это дискриминатор для всей секции J.

Измерено 19/19, неизмеренных нет.

ГЛАВНОЕ: вердикты по КЛАССУ поведения совпали с таблицей 1b225c2e для всех 19 позиций.
X13 этот набор не затронул — гипотеза подтверждена измерением, а не унаследована.

## M7 — молчащие промахи фильтров
Молчат: 100, 101, 102, 103 (digest = BASE, находки остаются видимы — не опасно),
106, 107 (страдает только формат вывода; дискриминатор: известный --format-opt ключ
реально меняет limit, неизвестный — нет).
ОПАСНО молчат: 104 (--namespace не совпал) и 105 (--class не совпал) — полный отчёт,
coverage.complete: true, 0 находок, exit 0. Неотличимо от «код действительно чист».
Дискриминатор: тот же флаг с реальным значением даёт непустой отличный digest.
Не молчат: 108, 109, 110 — refuse 3.

## M10 — baseline
111, 113 (добавленный ключ/поле) — silent, baseline применяется как обычно.
112, 116, 118 — refuse 3, тексты дословные («entries must be an object»,
«Unsupported baseline version: 99. Expected version 13», «Baseline file not found»).
114 (channel→chanel) — warn, все записи побились, находки РЕАЛЬНО вернулись (digest=BASE).
115 (magnitudes→magnitude) — warn, но текст называет "count" — ключ, которого в файле нет.
117 (scope не покрыт) — warn + пояснение, exit 0.

## M13
110 — refuse 3, но сообщение называет CLI-флаг --exclude-health, хотя ключ пришёл
из YAML exclude_health. Дефект подтверждён на текущем дереве.

## Допущения
EMPTY собран пустым каталогом, а не списком disabled_rules (первая попытка упала на
несуществующем в реестре cohesion.tcc). Для цели контроля достаточно.
Числовые расхождения n (5 здесь против 7 в таблице) — эффект другого пробника-инстанса,
не поведенческая регрессия.

## Замечание оркестратора
Агент не смог записать файл с именем report.md — запись блокируется. Содержимое
перенесено сюда оркестратором из результата агента.
