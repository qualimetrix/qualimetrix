--- candidate
+++ reference (mapped)
@@ -5,9 +5,9 @@
-  Dimension          Score   Status        Coverage  Thresholds
-  ─────────────────────────────────────────────────────────────────
-  Complexity         65.7%   ▼ Critical        100%  warn < 100  err < 100
-  Cohesion           97.6%   ▼ Critical        100%  warn < 100  err < 100
-  Coupling           80.0%   ▼ Critical        100%  warn < 100  err < 100
-  Typing             56.2%   ▼ Critical         n/a  warn < 100  err < 100
-  Maintainability    71.5%   ▼ Critical        100%  warn < 100  err < 100
-  ─────────────────────────────────────────────────────────────────
-  Overall            75.1%   ▼ Critical         n/a  warn < 100  err < 100
+  Dimension          Score   Status        Thresholds
+  ───────────────────────────────────────────────────────
+  Complexity         65.7%   ▼ Critical    warn < 100  err < 100
+  Cohesion           97.6%   ▼ Critical    warn < 100  err < 100
+  Coupling           80.0%   ▼ Critical    warn < 100  err < 100
+  Typing             56.2%   ▼ Critical    warn < 100  err < 100
+  Maintainability    71.5%   ▼ Critical    warn < 100  err < 100
+  ───────────────────────────────────────────────────────
+  Overall            75.1%   ▼ Critical    warn < 100  err < 100
@@ -35,2 +35,2 @@
-    Computed over 4 of 4 namespaces declaring a type (100%), from coupling.distance-own.count
-    Distance (own)                0.8   (target: below 0.3) — poor balance of abstraction and stability in its own classes
+    Computed over 4 of 4 leaf namespaces (100%), from coupling.distance.count
+    Distance                      0.8   (target: below 0.3) — poor balance of abstraction and stability
