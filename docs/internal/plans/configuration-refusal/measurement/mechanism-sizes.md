# Размеры механизмов, пересиятые на 1513bf67 (X14, волна измерения)

Источники: m/A/report.md (30–75), m/B/report.md (6, 76–99), m/C/verdicts.md (100–118),
m/D/report.md (4,5,10,25,26,27,119–133 + маршруты M6 + карта чтения конфига).
Каждый пакет пересиял СВОИ контроли на своём пробнике; все четыре набора контролей
различимы. Абсолютные дайджесты между пакетами не сопоставимы — пробники разные.

## Закрыто заходом X13 (перепроверено, не унаследовано)
23 позиции перешли из warn/silent в refuse 3: 30,31,32,34,35,36,37,38,40,41,43,48,52,
53,54,55,56,57,58,59,72,73,74.
M2 закрыт по измерению: enumerate-rule-option-keys.php даёт declared_not_read пустым
у всех 35 классов; 3 строки read_not_declared совпадают со слепыми пятнами AST-читателя.

## Внесено заходом X13 (новое, в перечислении отсутствует)
#51 — под `-q` отказ теперь exit 3 при 0 байт в ОБЕИХ трубах. Был warn + exit 0.
Сигнал остался только в коде возврата; объяснения нет вовсе.

## Опровергнуто
#45 suppress_namespace_channels НЕ no-op. Сопоставляется только с SymbolLevel::Namespace_
(RuleNamespaceExclusionProvider::isChannelExcluded). На size.class-count: 1 -> 0 -> 1.
Прежний вердикт был испорченным наблюдением (пробник на правиле без namespace-уровня).
Остаток уже: ключ принимается молча на правиле, у которого namespace-канала нет вовсе.

## Открытые механизмы с ИЗМЕРЕННЫМ размером

M6 маршрутизация отказа — 14 маршрутов (перечисление называло 5), 3 кода (1, 3, 255),
  3 варианта потока (stderr / stdout / оба сразу). Суть: код возврата определяется не
  классом ошибки, а тем, какая команда её поймала. Худшее: R10 — TypeError в `directives`
  не ловится нигде, exit 255, полный PHP-трейс в оба потока сразу.
  Карта чтения конфига снята по всем 9 командам: rules, graph:export, hook:* не читают
  qmx.yaml вовсе (доказано двусторонним дискриминатором, не только битым файлом).

M5 computed_metrics не перебирает ключи — 8 позиций, не изменились.
  Крах #88: ComputedMetricOverrideReader.php:134 -> :138 -> :193.
  Молчание health.complexity: тот же файл :229 (array_key_exists вместо перебора);
  #89 идёт другим путём — ComputedMetricsConfigResolver.php:53-55.

M3 написания — вырос против таблицы. Помимо 4,5,16,33,61,91: нормализатор возвращает
  автору НЕ ТО написание в текстах шести отказов (34 cALLABLE, 36 maxDistanceWarning,
  43 suppressNamespaceChanels, 48 excludePaths, 56 wARNING, 57 maxWarning).

M13 сообщение адресует не то — 25,26,27,82,110, не изменились.
M7 фильтры промахиваются молча — 100-107, не изменились. ОПАСНЫ 104,105:
  полный отчёт, coverage.complete true, 0 находок, exit 0.
M8 парсер --rule-opt — 71 (молча) и 75 (ложный текст), не изменились.
M9 инлайн-директивы — инверсия наблюдателей подтверждена в ОБЕ стороны (119, 127).
  Сверх: при 122/123 `directives` печатает «No inline directives», хотя check директиву видит.
M10 baseline читает по имени — 111,113 silent; 115 текст называет "count", ключа нет в файле.
M11 узнанный ключ делает больше/меньше — 60,64,65,66 остались молчащими.
M12 обнаружение конфига — 10, плюс полная карта из D (три команды не читают конфиг).

## Хвосты X13, подтверждённые оркестратором машинно
4 записи ратчета не совпадают ни с чем (CognitiveComplexity/Complexity/Npath/Distance
Options::fromArray), selfcheck при этом exit 0, строка на stderr.
RuleOptionsFactory: WMC 56, max CCN метода 10.
03-refusal-at-every-depth.md:389 «stdout left parseable» — неверно: на отказе stdout 0 байт.
RuleOptionKeyDoorSymmetryTest исполняется (сьют Infrastructure берёт каталог целиком).
Гейт по M6 не свидетельствует: все 16 кейсов корпуса — валидные аналитические прогоны.
